# ─── 学科助教系统 Edu-TA — Python 依赖（精简版） ──────────
# 已移除 sentence-transformers / torch 等重型依赖，
# Embedding 改用 API 调用 + 轻量降级方案。
# 安装：pip install -r requirements.txt

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0
openai>=1.55.0
numpy==1.24.3
pypdf==3.17.4
python-docx==1.1.0
python-multipart==0.0.6
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
Pillow==10.2.0
