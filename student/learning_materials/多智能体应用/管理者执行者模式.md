# 管理者-执行者模式（Manager-Worker）

## 引言

在现实世界中，几乎所有高效的团队都遵循一个基本原则：有人做决策和分工，有人专注执行。软件开发团队有Tech Lead和开发工程师，建筑工队有工头和工人，军队有指挥官和士兵。这种分工模式在人类协作中经历了数千年的验证，其核心理念同样适用于AI多智能体系统。

**管理者-执行者模式（Manager-Worker Pattern）** 是多智能体系统中最经典、最广泛使用的架构模式。它通过引入一个专门的"管理者"Agent来分解任务、分配工作、追踪进度、汇总结果，而多个"执行者"Agent则专注于完成各自的子任务。

---

## 架构设计

### 基本架构

```
                           ┌──────────────────┐
                           │   Manager Agent  │
                           │                  │
                           │ • 任务分析分解    │
                           │ • Worker调度     │
                           │ • 进度追踪       │
                           │ • 结果汇总整合    │
                           │ • 异常处理       │
                           └────────┬─────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
              ┌─────▼─────┐  ┌─────▼─────┐  ┌─────▼─────┐
              │ Worker A  │  │ Worker B  │  │ Worker C  │
              │           │  │           │  │           │
              │ 专注领域A │  │ 专注领域B │  │ 专注领域C │
              │ 工具集A   │  │ 工具集B   │  │ 工具集C   │
              └───────────┘  └───────────┘  └───────────┘
```

### 角色定义

| 角色 | 职责 | 能力要求 | 典型配置 |
|------|------|---------|---------|
| Manager | 任务分解、分配、追踪、汇总、异常处理 | 强推理、全局视野、组织能力 | 强模型 + 无工具限制 |
| Worker | 接收指定任务、专注执行、汇报结果 | 专业深度、专注力 | 领域特定模型 + 领域工具集 |

**Image-Prompt(英文绘图词):** Flat-design 2D vector illustration of the Manager-Worker architecture. Top: a "Manager Agent" block with five responsibility labels around it: "Task Decomposition" (puzzle pieces), "Worker Scheduling" (calendar/assignment icon), "Progress Tracking" (progress bar), "Result Aggregation" (merge arrows), "Exception Handling" (warning shield). Below, three horizontal "Worker" blocks (Worker A/B/C) each showing their focus domain and tool set. Connecting lines from Manager to each Worker with bidirectional arrows showing task assignment and result reporting. Tech blue #409EFF primary, white background, clean hierarchical diagram.

---

## Manager的职责详解

Manager是整个模式的核心，其决策质量直接决定系统输出的质量。Manager需要具备"指挥官的头脑"。

### 1. 任务分解（Task Decomposition）

Manager首先需要将用户的复杂任务分解为多个可独立执行的子任务。

```python
class ManagerAgent:
    def decompose_task(self, user_request: str) -> list[SubTask]:
        """
        将用户请求分解为子任务列表。

        分解策略：
        1. 识别任务的独立维度（如：技术分析、市场分析、财务分析）
        2. 识别任务的阶段依赖（如：必须先调研才能写报告）
        3. 评估每个子任务需要的技能和工具
        """
        decomposition_prompt = f"""
        你是一个项目管理专家。请将以下任务分解为可独立执行的子任务。

        原始任务：{user_request}

        分解要求：
        1. 每个子任务应该是一个独立、完整的工作单元
        2. 标注子任务之间的依赖关系
        3. 估算每个子任务的复杂度（low/medium/high）
        4. 标注每个子任务需要的专业技能

        输出JSON格式：
        {{
            "subtasks": [
                {{
                    "id": "task_1",
                    "description": "...",
                    "required_skill": "research",
                    "complexity": "medium",
                    "dependencies": [],
                    "estimated_tokens": 1000
                }}
            ]
        }}
        """
        response = self.llm.invoke(decomposition_prompt)
        return self._parse_subtasks(response)
```

### 2. Worker匹配与任务分配

分解任务后，Manager需要将每个子任务分配给最合适的Worker。

```python
class ManagerAgent:
    def __init__(self):
        self.worker_registry = {}  # skill -> [worker_ids]

    def register_worker(self, worker_id: str, skills: list[str],
                        capacity: int):
        """注册Worker及其能力"""
        for skill in skills:
            if skill not in self.worker_registry:
                self.worker_registry[skill] = []
            self.worker_registry[skill].append({
                "id": worker_id,
                "capacity": capacity,
                "current_load": 0
            })

    def assign_task(self, subtask: SubTask) -> str:
        """为子任务选择最合适的Worker"""
        required_skill = subtask.required_skill
        candidates = self.worker_registry.get(required_skill, [])

        if not candidates:
            # 没有完全匹配的Worker，寻找最接近的
            return self._find_best_fallback(subtask, required_skill)

        # 选择负载最低的Worker
        best_worker = min(candidates,
                         key=lambda w: w["current_load"])
        best_worker["current_load"] += 1
        return best_worker["id"]

    def _find_best_fallback(self, subtask, required_skill):
        """当没有精确匹配的Worker时的降级策略"""
        # 使用LLM判断哪个可用Worker最适合
        available_skills = list(self.worker_registry.keys())
        prompt = f"""
        任务需要技能: {required_skill}
        可用技能列表: {available_skills}
        请选择最接近的技能来执行此任务，或建议将任务进一步分解。
        """
        # ... LLM推理选择
```

### 3. 进度追踪（Progress Tracking）

Manager需要实时监控各Worker的执行状态，及时发现并处理异常。

```python
from enum import Enum
from datetime import datetime
import asyncio

class TaskStatus(Enum):
    PENDING = "pending"
    ASSIGNED = "assigned"
    IN_PROGRESS = "in_progress"
    BLOCKED = "blocked"
    COMPLETED = "completed"
    FAILED = "failed"

class TaskTracker:
    def __init__(self):
        self.tasks = {}  # task_id -> TaskInfo
        self.dependency_graph = {}  # task_id -> [dependent_task_ids]

    def update_status(self, task_id: str, status: TaskStatus,
                      metadata: dict = None):
        """更新任务状态"""
        task = self.tasks[task_id]
        task.status = status
        task.updated_at = datetime.now()
        if metadata:
            task.metadata.update(metadata)

        # 检查依赖任务的解锁
        if status == TaskStatus.COMPLETED:
            self._unblock_dependents(task_id)

    def _unblock_dependents(self, completed_task_id: str):
        """当任务完成时，解除依赖它的任务的阻塞状态"""
        dependents = self.dependency_graph.get(completed_task_id, [])
        for dep_id in dependents:
            dep_task = self.tasks[dep_id]
            # 检查所有依赖是否都已完成
            if all(self.tasks[d].status == TaskStatus.COMPLETED
                   for d in dep_task.dependencies):
                dep_task.status = TaskStatus.PENDING

    def get_overall_progress(self) -> dict:
        """获取整体进度报告"""
        total = len(self.tasks)
        completed = sum(1 for t in self.tasks.values()
                       if t.status == TaskStatus.COMPLETED)
        failed = sum(1 for t in self.tasks.values()
                    if t.status == TaskStatus.FAILED)
        return {
            "total": total,
            "completed": completed,
            "failed": failed,
            "in_progress": total - completed - failed,
            "completion_pct": (completed / total * 100) if total > 0 else 0
        }
```

### 4. 结果汇总（Result Aggregation）

这是Manager最体现价值的工作——将零散的Worker输出编织成连贯的整体。

```python
class ManagerAgent:
    def aggregate_results(self, task_id: str,
                          worker_results: dict[str, str]) -> str:
        """
        汇总多个Worker的执行结果。

        关键挑战：
        1. 处理结果间的矛盾和不一致
        2. 消除冗余信息
        3. 填充空白和缺口
        4. 统一风格和格式
        """
        aggregation_prompt = f"""
        你是最终报告的整合编辑。以下是不同分析师的独立输出，请将它们整合为一份
        连贯、一致、无冗余的最终报告。

        各Worker输出：
        {self._format_worker_outputs(worker_results)}

        整合原则：
        1. 发现并解决事实矛盾（标注存疑点）
        2. 按逻辑结构重组内容
        3. 统一术语和表达风格
        4. 标注需要人工确认的地方
        5. 保留所有关键发现，不遗漏重要信息

        输出格式：Markdown
        """
        return self.llm.invoke(aggregation_prompt)

    def handle_conflicts(self, result_a: str, result_b: str) -> str:
        """当两个Worker的输出矛盾时的冲突解决"""
        conflict_prompt = f"""
        以下是两个分析师的输出，它们在某些方面存在矛盾。请分析矛盾点并给出综合结论。

        分析师A输出：{result_a}

        分析师B输出：{result_b}

        请：
        1. 列出所有矛盾点
        2. 对每个矛盾，判断哪个结论更可靠（附理由）
        3. 如果有无法判断的，标注为"待人工确认"
        """
        return self.llm.invoke(conflict_prompt)
```

### 5. 异常处理（Exception Handling）

```python
class ManagerAgent:
    async def handle_worker_failure(self, failed_task: SubTask,
                                     error_info: str):
        """处理Worker执行失败的情况"""
        strategies = [
            "retry_same_worker",    # 同Worker重试
            "reassign_new_worker",  # 换Worker重试
            "task_simplify",        # 简化任务
            "task_skip",            # 跳过（非关键任务）
            "escalate_to_human",    # 升级给人类
        ]

        # 根据错误类型选择策略
        if self._is_transient_error(error_info):
            return await self._retry_task(failed_task)
        elif self._is_skill_mismatch(error_info):
            return await self._reassign_task(failed_task)
        elif failed_task.is_critical:
            return await self._escalate(failed_task)
        else:
            return self._skip_and_compensate(failed_task)
```

**Image-Prompt(英文绘图词):** Flat-design 2D vector illustration of the Manager's five core responsibilities as a pentagon diagram. Each vertex labeled with a responsibility: "Task Decomposition" (large task splitting into small blocks), "Worker Matching" (puzzle piece fitting into slot), "Progress Tracking" (dashboard with progress bars and status indicators), "Result Aggregation" (multiple inputs merging into one output), "Exception Handling" (error shield with fallback paths). Center of pentagon: "Manager Agent" with brain icon. Connecting lines show the flow between responsibilities. Tech blue #409EFF primary, white background.

---

## Worker的职责详解

Worker的设计哲学是"少即是多"——每个Worker应该尽可能聚焦和专业化。

### 1. 专注执行

Worker接收清晰定义的任务，不应参与任务分解或策略决策。

```python
class WorkerAgent:
    def __init__(self, name: str, role: str, expertise: str,
                 tools: list, model: str):
        self.name = name
        self.role = role
        self.expertise = expertise
        self.tools = tools
        self.model = model

        self.system_prompt = f"""
        你是{name}，担任{role}角色。
        你的专业领域是：{expertise}

        工作原则：
        1. 只做你被要求做的事，不越界
        2. 遇到不确定的信息，标注为"不确定"而非猜测
        3. 输出结构化结果，便于Manager整合
        4. 如果任务超出你的能力范围，立即报告
        """

    async def execute(self, task: SubTask) -> WorkerResult:
        """执行一个子任务"""
        try:
            # 验证任务是否在自己的能力范围内
            self._validate_task_scope(task)

            # 执行任务
            result = await self._do_work(task)

            return WorkerResult(
                worker_id=self.name,
                task_id=task.id,
                status="success",
                output=result,
                metadata={
                    "tools_used": self._get_used_tools(),
                    "confidence": self._assess_confidence(result)
                }
            )
        except OutOfScopeError as e:
            return WorkerResult(
                worker_id=self.name,
                task_id=task.id,
                status="out_of_scope",
                output=str(e),
                metadata={}
            )
        except Exception as e:
            return WorkerResult(
                worker_id=self.name,
                task_id=task.id,
                status="failed",
                output=str(e),
                metadata={"error_type": type(e).__name__}
            )

    def _validate_task_scope(self, task: SubTask):
        """验证任务是否在Worker的能力范围内"""
        if task.required_skill not in self.skills:
            raise OutOfScopeError(
                f"任务需要 {task.required_skill} 技能，"
                f"但 {self.name} 擅长的是 {self.skills}"
            )
```

### 2. 汇报结果

Worker需要按照约定格式汇报结果，使Manager能够高效整合。

```python
class WorkerResult:
    """Worker返回结果的标准化格式"""
    def __init__(self, worker_id: str, task_id: str, status: str,
                 output: str, metadata: dict):
        self.worker_id = worker_id
        self.task_id = task_id
        self.status = status
        self.output = output
        self.metadata = metadata
        self.timestamp = datetime.now()

    def to_dict(self) -> dict:
        return {
            "worker_id": self.worker_id,
            "task_id": self.task_id,
            "status": self.status,
            "output": self.output,
            "confidence": self.metadata.get("confidence", "unknown"),
            "tools_used": self.metadata.get("tools_used", []),
            "token_usage": self.metadata.get("token_usage", 0),
            "duration_ms": self.metadata.get("duration_ms", 0),
            "timestamp": self.timestamp.isoformat()
        }
```

### 3. 自我评估

高级Worker应具备自我评估能力，对结果质量进行评分。

```python
class WorkerAgent:
    def _assess_confidence(self, output: str) -> str:
        """评估输出置信度"""
        assessment = self.llm.invoke(f"""
        请评估以下输出内容的置信度：

        输出内容：{output}

        评分标准：
        - high: 所有信息有坚实依据，无推测成分
        - medium: 大部分有依据，少量推测但已标注
        - low: 信息不完整或存在较大推测空间

        请只返回一个词：high/medium/low
        """)
        return assessment.strip().lower()
```

**Image-Prompt(英文绘图词):** Flat-design 2D vector illustration showing Worker agent responsibilities. Three Worker blocks in a row, each showing: "Focused Execution" (magnifying glass over a single task), "Result Reporting" (structured report document with JSON format visible), "Self-Assessment" (confidence meter gauge showing high/medium/low). Each Worker has a "Scope Boundary" dashed line around it, emphasizing they stay within their domain. An "Out of Scope" redirection arrow pointing back to Manager. Tech blue #409EFF primary, white background.

---

## 消息流转

Manager-Worker模式中的完整消息流转过程：

```
                        ┌─────────────┐
         ①请求          │   Manager   │
     ─────────────────>│             │
                        └──────┬──────┘
                               │
                    ②任务分解与分配
                               │
              ┌────────────────┼────────────────┐
              │                │                │
        ③任务消息         ③任务消息         ③任务消息
              │                │                │
     ┌────────▼──────┐ ┌──────▼────────┐ ┌──────▼────────┐
     │   Worker A    │ │   Worker B    │ │   Worker C    │
     └────────┬──────┘ └──────┬────────┘ └──────┬────────┘
              │                │                │
        ④状态更新         ④状态更新         ④状态更新
              │                │                │
        ⑤结果汇报         ⑤结果汇报         ⑤结果汇报
              │                │                │
              └────────────────┼────────────────┘
                               │
                        ┌──────▼──────┐
                        │   Manager   │
                        │  ⑥汇总整合  │
                        └──────┬──────┘
                               │
                         ⑦最终输出
```

### 消息格式示例

**任务分配消息（Manager -> Worker）**：

```json
{
  "message_type": "task_assignment",
  "message_id": "msg_001",
  "sender": "manager_01",
  "receiver": "worker_research_01",
  "timestamp": "2024-07-09T10:00:00Z",
  "content": {
    "task_id": "task_research_market",
    "task_type": "research",
    "description": "调研2024年新能源汽车市场趋势",
    "parameters": {
      "focus_areas": ["中国市场", "欧洲市场"],
      "key_players": ["比亚迪", "特斯拉"],
      "time_range": "2024-Q1 to 2024-Q3",
      "output_format": "structured_report"
    },
    "priority": "high",
    "deadline": "2024-07-09T10:05:00Z",
    "context": {
      "parent_task": "industry_analysis_report",
      "related_tasks": ["task_tech_analysis", "task_financial_analysis"]
    }
  }
}
```

**状态更新消息（Worker -> Manager）**：

```json
{
  "message_type": "status_update",
  "message_id": "msg_002",
  "sender": "worker_research_01",
  "receiver": "manager_01",
  "timestamp": "2024-07-09T10:02:30Z",
  "content": {
    "task_id": "task_research_market",
    "status": "in_progress",
    "progress_pct": 60,
    "current_step": "收集欧洲市场数据",
    "issues": [],
    "estimated_completion": "2024-07-09T10:04:00Z"
  }
}
```

**Image-Prompt(英文绘图词):** Flat-design 2D vector illustration of the complete message flow in Manager-Worker pattern. Numbered steps 1-7 in sequence: 1) User Request arrow to Manager, 2) Manager decomposes and assigns, 3) Task Assignment messages (structured JSON envelopes) going to Workers A/B/C, 4) Status Updates (progress indicators) coming back from Workers, 5) Result Reports (document icons) from Workers to Manager, 6) Manager aggregates results, 7) Final Output to User. A legend showing message types: task_assignment (blue), status_update (yellow), task_result (green). Tech blue #409EFF primary, white background.

---

## 代码示例

### CrewAI实现

```python
from crewai import Agent, Task, Crew, Process

# ============ Manager Agent ============
manager = Agent(
    role="项目经理",
    goal="将用户需求分解为可执行的子任务，分配给合适的专家，"
         "追踪进度并整合最终成果",
    backstory=(
        "你是一位经验丰富的技术项目经理，曾在多家科技公司"
        "领导过复杂的跨领域项目。你擅长将模糊的高层需求拆解"
        "为清晰可执行的子任务，并确保每个团队成员都在做最适"
        "合他们的事。"
    ),
    allow_delegation=True,  # 允许Manager向Worker委派任务
    verbose=True
)

# ============ Worker Agents ============
researcher = Agent(
    role="市场研究员",
    goal="收集和分析市场数据，提供数据驱动的洞察",
    backstory="你是一位资深市场研究员，擅长定量和定性分析",
    tools=[WebSearchTool(), DataAnalysisTool()],
    verbose=True
)

analyst = Agent(
    role="技术分析师",
    goal="分析技术趋势和竞争格局，评估技术可行性",
    backstory="你是一位技术分析师，10年行业经验",
    tools=[CodeAnalysisTool(), TechRadarTool()],
    verbose=True
)

writer = Agent(
    role="报告撰写人",
    goal="将分析和研究结果整合为专业、清晰的报告",
    backstory="你是一位资深技术撰稿人，曾为多家顶级咨询公司服务",
    tools=[DocumentTemplateTool()],
    verbose=True
)

# ============ 定义任务 ============
# Manager的任务是整体管理
manage_task = Task(
    description=(
        "基于以下用户需求，协调团队完成一份行业分析报告：\n"
        "用户需求：分析2024年新能源汽车行业趋势，关注中国和"
        "欧洲市场，重点分析比亚迪和特斯拉。报告需要包含市场"
        "规模、技术路线对比、竞争格局和未来预测。\n\n"
        "你需要：\n"
        "1. 将需求分解为子任务\n"
        "2. 分配合适的专家执行\n"
        "3. 监控进度并处理异常\n"
        "4. 整合所有结果为一篇完整的报告"
    ),
    agent=manager,
    expected_output="一份完整的行业分析Markdown报告"
)

# Worker任务（由Manager动态委派，这里只是预定义）
research_task = Task(
    description="研究市场数据和趋势",
    agent=researcher,
    expected_output="市场研究报告"
)

analysis_task = Task(
    description="分析技术趋势和竞争格局",
    agent=analyst,
    expected_output="技术分析报告"
)

writing_task = Task(
    description="整合研究结果撰写最终报告",
    agent=writer,
    expected_output="最终报告"
)

# ============ 创建Crew ============
crew = Crew(
    agents=[manager, researcher, analyst, writer],
    tasks=[manage_task],
    process=Process.hierarchical,  # 层级模式 = Manager-Worker
    verbose=True
)

# 启动
result = crew.kickoff()
print(result)
```

### AutoGen实现

```python
from autogen import AssistantAgent, UserProxyAgent, GroupChat, GroupChatManager

# ============ 配置LLM ============
config_list = [{
    "model": "claude-sonnet-4-20250514",
    "api_key": "your-api-key",
    "api_type": "anthropic"
}]

llm_config = {"config_list": config_list}

# ============ Manager Agent ============
manager = AssistantAgent(
    name="Manager",
    system_message=(
        "你是项目经理。你的职责：\n"
        "1. 分析用户请求，将其分解为子任务\n"
        "2. 将子任务分配给合适的专家(Researcher/Analyst/Writer)\n"
        "3. 追踪进度，确保所有子任务完成\n"
        "4. 检查各专家的输出质量，如有问题要求修正\n"
        "5. 整合所有结果为一篇完整的报告\n\n"
        "通信规则：\n"
        "- 用 @Researcher 分配研究任务\n"
        "- 用 @Analyst 分配分析任务\n"
        "- 用 @Writer 分配写作任务\n"
        "- 收到所有结果后，向 UserProxy 发送最终报告"
    ),
    llm_config=llm_config
)

# ============ Worker Agents ============
researcher = AssistantAgent(
    name="Researcher",
    system_message=(
        "你是市场研究员。被@后，执行指定的研究任务。\n"
        "工具：web_search, document_read\n"
        "输出格式：结构化JSON，包含数据来源和时间戳\n"
        "完成后用 'TASK_COMPLETE: [你的结果]' 格式汇报"
    ),
    llm_config=llm_config
)

analyst = AssistantAgent(
    name="Analyst",
    system_message=(
        "你是技术分析师。被@后，执行指定的分析任务。\n"
        "工具：data_analysis, chart_generation\n"
        "输出格式：结构化分析，包含量化指标\n"
        "完成后用 'TASK_COMPLETE: [你的结果]' 格式汇报"
    ),
    llm_config=llm_config
)

writer = AssistantAgent(
    name="Writer",
    system_message=(
        "你是报告撰写人。被@后，基于提供的研究和分析结果撰写报告。\n"
        "输出格式：Markdown，专业排版\n"
        "完成后用 'TASK_COMPLETE: [你的结果]' 格式汇报"
    ),
    llm_config=llm_config
)

# ============ UserProxy ============
user_proxy = UserProxyAgent(
    name="UserProxy",
    human_input_mode="NEVER",
    code_execution_config=False,
    system_message="你代表用户。收到最终报告后回复 'TERMINATE'。"
)

# ============ GroupChat ============
groupchat = GroupChat(
    agents=[user_proxy, manager, researcher, analyst, writer],
    messages=[],
    max_round=20,
    speaker_selection_method="auto"  # AutoGen自动选择下一位发言者
)

chat_manager = GroupChatManager(
    groupchat=groupchat,
    llm_config=llm_config
)

# ============ 启动对话 ============
user_proxy.initiate_chat(
    chat_manager,
    message=(
        "请协调团队完成一份分析报告：分析2024年新能源汽车行业趋势，"
        "关注中国和欧洲市场，重点分析比亚迪和特斯拉。"
    )
)
```

---

## 优缺点分析

### 优势

| 优势 | 详细说明 |
|------|---------|
| **结构清晰** | 角色分明，谁负责什么都一目了然，降低认知负担 |
| **质量控制** | Manager可以审查Worker的产出，退回不合格的结果 |
| **动态调整** | Manager可以根据中间结果动态改变后续策略 |
| **责任明确** | 出现问题时容易定位——是Manager分解不当还是Worker执行不力 |
| **天然支持流水线** | 多阶段任务（研究→分析→写作→审校）天然适配 |
| **新手友好** | 与人类团队管理经验高度吻合，容易理解和设计 |

### 劣势

| 劣势 | 详细说明 | 缓解策略 |
|------|---------|---------|
| **Manager单点瓶颈** | Manager的推理能力限制整个系统的上限 | 使用最强的模型作为Manager |
| **通信开销大** | Manager与每个Worker都需要来回通信 | 批量分配+异步通信 |
| **Manager负载过高** | 任务分解、分配、追踪、汇总全部压在Manager身上 | Manager本身可有多实例 |
| **层级僵化** | Manager固定在工作流顶端，缺乏灵活性 | 允许Worker自主发起协作 |
| **信息衰减** | Manager作为信息中转站，可能遗漏或曲解信息 | 结构化消息+共享上下文 |
| **扩展性受限** | Worker数量增加时，Manager的协调负担线性增长 | 引入子Manager（分层管理） |

---

## 变体模式

### 1. 分层Manager-Worker（Hierarchical Manager-Worker）

当Worker数量过多时，引入中间管理层：

```
                 ┌──────────────┐
                 │ 总Manager    │
                 └──────┬───────┘
          ┌─────────────┼─────────────┐
    ┌─────▼─────┐ ┌─────▼─────┐ ┌─────▼─────┐
    │子Manager A│ │子Manager B│ │子Manager C│
    │(研究领域)  │ │(分析领域)  │ │(写作领域)  │
    └─────┬─────┘ └─────┬─────┘ └─────┬─────┘
      ┌────┴────┐    ┌────┴────┐    ┌────┴────┐
   Worker  Worker  Worker  Worker  Worker  Worker
```

### 2. Manager-Reviewer-Worker

引入独立的Reviewer Agent，减轻Manager的质量审查负担：

```
Manager → 分配任务 → Worker → 产出结果
                              ↓
                          Reviewer → 审查通过？→ Yes → Manager汇总
                              ↓ No
                          退回Worker修改
```

### 3. 轮值Manager

不同阶段由不同的Agent担任Manager：
- 分解阶段：战略分析师担任Manager
- 执行阶段：技术Leader担任Manager
- 整合阶段：编辑/撰稿人担任Manager

**Image-Prompt(英文绘图词):** Flat-design 2D vector illustration showing three Manager-Worker variant patterns. Top "Hierarchical Manager-Worker": three-level tree (Top Manager → Sub-Managers A/B/C → Workers). Middle "Manager-Reviewer-Worker": Manager → Worker → Reviewer (checkmark/X gate) → back to Worker or forward to Manager. Bottom "Rotating Manager": a circular diagram showing three phases with different agents taking the Manager role (Strategy Analyst → Tech Lead → Editor), connected by rotation arrows. Tech blue #409EFF primary, white background, three-panel layout.

---

## 最佳实践

1. **Manager用最强模型**：Manager的推理能力是系统质量的瓶颈
2. **Worker够用就好**：根据子任务复杂度选择模型，节省成本
3. **明确的信息接口**：定义清楚Manager和Worker之间的消息格式
4. **超时和重试**：Worker可能卡住，Manager需要有超时处理
5. **分级日志**：记录每个决策和通信，便于调试
6. **渐进式复杂度**：从2-3个Worker开始，验证通过后再扩展
7. **人工回环**：关键决策点允许人工介入审批

---

## 小结

Manager-Worker模式是多智能体系统中最成熟、最实用的协作模式。它的核心思想——"有人决策，有人执行"——与人类组织管理的经验高度吻合，因此特别容易理解和上手。

**适用判断**：
- 任务有明显的层级结构？用Manager-Worker
- 需要一个Agent统揽全局？用Manager-Worker
- Worker数量不超过10个？直接用
- Worker数量超过10个？考虑分层Manager-Worker
- Manager成为瓶颈？用最强模型或引入子Manager

记住：Manager-Worker虽好，但不要滥用。如果任务本身不需要分解和协调，单Agent就是更优方案。
