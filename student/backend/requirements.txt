# ============================================
# AI智能体学科学习平台 — 后端依赖
# Python >= 3.11
# ============================================

# --- Web 框架 & 服务器 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- 认证 & 安全 ---
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1

# --- 数据验证 ---
pydantic>=2.0.0

# --- HTTP & 表单 ---
python-multipart>=0.0.9
aiohttp>=3.9.0
httpx>=0.27.0

# --- LangChain AI ---
langchain-openai>=0.2.0
langchain-core>=0.3.0
langgraph>=1.0.0

# --- 测试（可选） ---
# playwright>=1.48.0   # 仅 test_streaming.py 使用，取消注释后需执行 playwright install

# --- 文件解析 ---
PyMuPDF>=1.24.0
python-pptx>=1.0.0
python-docx>=1.0.0
openpyxl>=3.1.0

# --- 图片OCR（提取图片中的文字，兼容所有纯文本LLM） ---
Pillow>=10.0.0
pytesseract>=0.3.0

# --- PostgreSQL 支持 ---
psycopg2-binary>=2.9.9

# --- RAG 向量数据库 & 云端嵌入 ---
chromadb>=0.5.0
dashscope>=1.20.0

# --- RAG 本地嵌入模型只放在本地虚拟环境，见 requirements-dev.txt ---
