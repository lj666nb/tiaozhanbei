[
  {
    "id": "1-1",
    "title": "理解 AI 智能体的官方学术定义",
    "module": "模块一 · 智能体基础通识",
    "description": "AI 智能体（Agent）的经典学术定义源自 Michael Wooldridge 和 Nicholas Jennings（1995）：\n智能体是一个位于环境中、能够通过传感器感知环境状态、并通过效应器对环境施加\n行动的计算机系统。\n\n本关要求你编写一个函数 `agent_definition_demo()`，返回一个包含 AI 智能体四大\n核心要素的字典：感知（perception）、决策（decision）、行动（action）、环境（environment）。\n每个要素对应一段简明解释。",
    "input_output": "输入：无\n输出：返回一个 dict，包含 perception、decision、action、environment 四个键",
    "skeleton_code": "# ==========你的代码开始==========\ndef agent_definition_demo():\n    \"\"\"\n    返回 AI 智能体四大核心要素及其解释。\n    \"\"\"\n    # TODO: 构建并返回字典\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: agent_definition_demo ═══\")\n    required_keys = {\"perception\", \"decision\", \"action\", \"environment\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = agent_definition_demo()\n        assert isinstance(result, dict)\n        assert required_keys.issubset(set(result.keys()))\n        assert isinstance(result[k], str) and len(result[k]) > 5\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: agent_definition_demo ═══\")\n    required_keys = {\"perception\", \"decision\", \"action\", \"environment\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = agent_definition_demo()\n        assert isinstance(result, dict)\n        assert required_keys.issubset(set(result.keys()))\n        assert isinstance(result[k], str) and len(result[k]) > 5\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_1(student_func):\n    result = student_func()\n    required_keys = {\"perception\", \"decision\", \"action\", \"environment\"}\n    assert isinstance(result, dict), \"返回值必须是字典\"\n    assert required_keys.issubset(set(result.keys())), f\"缺少必要键: {required_keys - set(result.keys())}\"\n    for k in required_keys:\n        assert isinstance(result[k], str) and len(result[k]) > 5, f\"键'{k}'的解释太短\"\n    return True, \"关卡通过：正确理解智能体四要素\""
  },
  {
    "id": "1-2",
    "title": "智能体 vs 传统 AI 应用的被动应答",
    "module": "模块一 · 智能体基础通识",
    "description": "传统 AI 应用（如推荐系统）是\"被动应答\"模式：用户请求 → 系统响应。\nAI 智能体则是\"主动决策\"模式：感知环境 → 自主规划 → 执行行动 → 观察反馈。\n\n本关要求你编写函数 `compare_passive_vs_agent(scenario)`，接收场景字符串参数\n\"recommend\" 或 \"agent\"，分别返回传统被动应答和智能体主动决策的处理流程列表。",
    "input_output": "输入：scenario（字符串，取值 \"recommend\" 或 \"agent\"）\n输出：返回一个 list[str]，每步一个字符串描述\n样例输入：\"recommend\"\n样例输出：[\"用户发出请求\", \"系统检索数据库\", \"返回推荐结果\"]",
    "skeleton_code": "# ==========你的代码开始==========\ndef compare_passive_vs_agent(scenario):\n    \"\"\"\n    scenario=\"recommend\": 返回被动应答流程\n    scenario=\"agent\":    返回智能体主动决策流程\n    \"\"\"\n    # TODO: 根据 scenario 返回对应的流程步骤列表\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: compare_passive_vs_agent ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = compare_passive_vs_agent(\"recommend\")\n        a = compare_passive_vs_agent(\"agent\")\n        assert isinstance(r, list) and len(r) >= 3\n        assert isinstance(a, list) and len(a) >= 4\n        assert \"用户\" in r[0] or \"请求\" in r[0]\n        assert any(\"感知\" in step or \"观察\" in step for step in a)\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: compare_passive_vs_agent ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = compare_passive_vs_agent(\"recommend\")\n        a = compare_passive_vs_agent(\"agent\")\n        assert isinstance(r, list) and len(r) >= 3\n        assert isinstance(a, list) and len(a) >= 4\n        assert \"用户\" in r[0] or \"请求\" in r[0]\n        assert any(\"感知\" in step or \"观察\" in step for step in a)\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_2(student_func):\n    r = student_func(\"recommend\")\n    assert isinstance(r, list) and len(r) >= 3, \"推荐场景至少3步流程\"\n    a = student_func(\"agent\")\n    assert isinstance(a, list) and len(a) >= 4, \"智能体场景至少4步流程\"\n    # 被动应答第一步应为用户请求\n    assert \"用户\" in r[0] or \"请求\" in r[0], \"被动应答应从用户请求开始\"\n    # 智能体流程应包含感知\n    assert any(\"感知\" in step or \"观察\" in step for step in a), \"智能体流程需包含感知环节\"\n    return True, \"关卡通过：正确区分被动应答与主动决策\""
  },
  {
    "id": "1-3",
    "title": "智能体 vs 聊天机器人的核心分界线",
    "module": "模块一 · 智能体基础通识",
    "description": "聊天机器人（ChatBot）和智能体的核心区别在于：ChatBot 只有\"对话\"一个能力\n通道，而智能体可以主动调用工具、执行代码、操控外部系统。\n\n编写函数 `classify_ai_system(features)`，接收一个特征集合（set），根据是否有\n工具调用（tool_calling）和环境操作（env_manipulation）能力来分类。返回 \"ChatBot\"\n或 \"Agent\"。",
    "input_output": "输入：features（set[str]，包含 tool_calling、env_manipulation、dialogue 等）\n输出：返回 \"ChatBot\" 或 \"Agent\"\n样例输入：{\"dialogue\", \"tool_calling\"}\n样例输出：\"Agent\"",
    "skeleton_code": "# ==========你的代码开始==========\ndef classify_ai_system(features):\n    \"\"\"\n    features: set[str] 包含系统的能力特征\n    返回: \"ChatBot\" 或 \"Agent\"\n    \"\"\"\n    # TODO: 根据特征判断系统类型\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: classify_ai_system ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert classify_ai_system({\"dialogue\"}) == \"ChatBot\"\n        assert classify_ai_system({\"dialogue\", \"tool_calling\"}) == \"Agent\"\n        assert classify_ai_system({\"dialogue\", \"env_manipulation\"}) == \"Agent\"\n        assert classify_ai_system({\"dialogue\", \"tool_calling\", \"env_manipulation\"}) == \"Agent\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: classify_ai_system ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert classify_ai_system({\"dialogue\"}) == \"ChatBot\"\n        assert classify_ai_system({\"dialogue\", \"tool_calling\"}) == \"Agent\"\n        assert classify_ai_system({\"dialogue\", \"env_manipulation\"}) == \"Agent\"\n        assert classify_ai_system({\"dialogue\", \"tool_calling\", \"env_manipulation\"}) == \"Agent\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_3(student_func):\n    assert student_func({\"dialogue\"}) == \"ChatBot\", \"只有对话功能应为ChatBot\"\n    assert student_func({\"dialogue\", \"tool_calling\"}) == \"Agent\", \"有工具调用应为Agent\"\n    assert student_func({\"dialogue\", \"env_manipulation\"}) == \"Agent\", \"有环境操作应为Agent\"\n    assert student_func({\"dialogue\", \"tool_calling\", \"env_manipulation\"}) == \"Agent\"\n    return True, \"关卡通过：正确区分ChatBot与Agent\""
  },
  {
    "id": "1-4",
    "title": "智能体 vs 工作流的适用场景",
    "module": "模块一 · 智能体基础通识",
    "description": "工作流（Workflow）是预定义的固定步骤流程，智能体则能根据环境反馈动态调整。\n两者适用场景不同：工作流适合标准化任务，智能体适合需要灵活决策的开放任务。\n\n编写函数 `recommend_approach(task_type)`，根据任务类型推荐使用 Workflow 或 Agent。\n规则：\n- 如果 task_type 为 \"invoice_processing\"（发票处理）或 \"data_etl\"，返回 \"Workflow\"\n- 如果 task_type 为 \"customer_service\" 或 \"research\"，返回 \"Agent\"\n- 其他情况返回 \"Unknown\"",
    "input_output": "输入：task_type（字符串）\n输出：\"Workflow\" / \"Agent\" / \"Unknown\"\n样例输入：\"research\"\n样例输出：\"Agent\"",
    "skeleton_code": "# ==========你的代码开始==========\ndef recommend_approach(task_type):\n    \"\"\"\n    根据任务类型推荐 Workflow 或 Agent。\n    \"\"\"\n    # TODO: 实现映射逻辑\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: recommend_approach ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert recommend_approach(\"invoice_processing\") == \"Workflow\"\n        assert recommend_approach(\"data_etl\") == \"Workflow\"\n        assert recommend_approach(\"customer_service\") == \"Agent\"\n        assert recommend_approach(\"research\") == \"Agent\"\n        assert recommend_approach(\"unknown_task\") == \"Unknown\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: recommend_approach ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert recommend_approach(\"invoice_processing\") == \"Workflow\"\n        assert recommend_approach(\"data_etl\") == \"Workflow\"\n        assert recommend_approach(\"customer_service\") == \"Agent\"\n        assert recommend_approach(\"research\") == \"Agent\"\n        assert recommend_approach(\"unknown_task\") == \"Unknown\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_4(student_func):\n    assert student_func(\"invoice_processing\") == \"Workflow\"\n    assert student_func(\"data_etl\") == \"Workflow\"\n    assert student_func(\"customer_service\") == \"Agent\"\n    assert student_func(\"research\") == \"Agent\"\n    assert student_func(\"unknown_task\") == \"Unknown\"\n    return True, \"关卡通过：正确区分配置场景\""
  },
  {
    "id": "1-5",
    "title": "智能体的五大核心特征",
    "module": "模块一 · 智能体基础通识",
    "description": "AI 智能体有五大核心特征：自主性（Autonomy）、感知力（Perception）、推理力\n（Reasoning）、规划力（Planning）、行动力（Action）。\n\n编写函数 `validate_agent_features(agent_config)`，接收一个包含 feature_scores\n的字典，检查五大特征是否都 >= 60 分（满分100）。返回通过的特征列表和未通过的\n特征列表。",
    "input_output": "输入：agent_config，格式 {\"feature_scores\": {\"Autonomy\": 80, \"Perception\": 70, ...}}\n输出：{\"passed\": [...], \"failed\": [...]}\n样例输入：{\"feature_scores\": {\"Autonomy\": 80, \"Perception\": 50, \"Reasoning\": 90, \"Planning\": 40, \"Action\": 75}}\n样例输出：{\"passed\": [\"Autonomy\", \"Reasoning\", \"Action\"], \"failed\": [\"Perception\", \"Planning\"]}",
    "skeleton_code": "# ==========你的代码开始==========\ndef validate_agent_features(agent_config):\n    \"\"\"\n    检查五大核心特征是否达标（>=60）。\n    返回 {\"passed\": [...], \"failed\": [...]}\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 遍历检查并分类\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: validate_agent_features ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = validate_agent_features({\"feature_scores\": {\"Autonomy\": 80, \"Perception\": 70, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 75}})\n        r2 = validate_agent_features({\"feature_scores\": {\"Autonomy\": 50, \"Perception\": 90, \"Reasoning\": 30, \"Planning\": 80, \"Action\": 55}})\n        assert r1[\"passed\"] == [\"Autonomy\", \"Perception\", \"Reasoning\", \"Planning\", \"Action\"], \"  断言失败\"\n        assert r1[\"failed\"] == [], \"  断言失败\"\n        assert set(r2[\"failed\"]) == {\"Autonomy\", \"Reasoning\", \"Action\"}, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: validate_agent_features ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = validate_agent_features({\"feature_scores\": {\"Autonomy\": 80, \"Perception\": 70, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 75}})\n        r2 = validate_agent_features({\"feature_scores\": {\"Autonomy\": 50, \"Perception\": 90, \"Reasoning\": 30, \"Planning\": 80, \"Action\": 55}})\n        assert r1[\"passed\"] == [\"Autonomy\", \"Perception\", \"Reasoning\", \"Planning\", \"Action\"], \"  断言失败\"\n        assert r1[\"failed\"] == [], \"  断言失败\"\n        assert set(r2[\"failed\"]) == {\"Autonomy\", \"Reasoning\", \"Action\"}, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_5(student_func):\n    # 全部通过\n    r1 = student_func({\"feature_scores\": {\"Autonomy\": 80, \"Perception\": 70, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 75}})\n    assert r1[\"passed\"] == [\"Autonomy\", \"Perception\", \"Reasoning\", \"Planning\", \"Action\"]\n    assert r1[\"failed\"] == []\n    # 部分失败\n    r2 = student_func({\"feature_scores\": {\"Autonomy\": 50, \"Perception\": 90, \"Reasoning\": 30, \"Planning\": 80, \"Action\": 55}})\n    assert set(r2[\"failed\"]) == {\"Autonomy\", \"Reasoning\", \"Action\"}\n    return True, \"关卡通过：正确识别五大核心特征\""
  },
  {
    "id": "1-6",
    "title": "智能体的辅助特征：记忆能力与学习能力",
    "module": "模块一 · 智能体基础通识",
    "description": "除五大核心特征外，高级智能体还具备：记忆能力（Memory）—— 存储历史交互以便\n未来检索；学习能力（Learning）—— 从经验中改进策略。\n\n编写函数 `assess_agent_level(features_dict)`，接收特征评分字典，判断智能体等级：\n- 五大核心特征全 >= 60 且 Memory >= 60 且 Learning >= 60 → \"L3-高级\"\n- 五大核心特征全 >= 60 → \"L2-标准\"\n- 否则 → \"L1-基础\"",
    "input_output": "输入：features_dict（dict，键为特征名，值为 0-100 的整数）\n输出：\"L3-高级\" / \"L2-标准\" / \"L1-基础\"",
    "skeleton_code": "# ==========你的代码开始==========\ndef assess_agent_level(features_dict):\n    \"\"\"\n    根据所有特征评分判断智能体等级。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 实现等级判断逻辑\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: assess_agent_level ═══\")\n    f1 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 80, \"Learning\": 75}\n    f2 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 30, \"Learning\": 20}\n    f3 = {\"Autonomy\": 40, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert assess_agent_level(f1) == \"L3-高级\"\n        assert assess_agent_level(f2) == \"L2-标准\"\n        assert assess_agent_level(f3) == \"L1-基础\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: assess_agent_level ═══\")\n    f1 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 80, \"Learning\": 75}\n    f2 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 30, \"Learning\": 20}\n    f3 = {\"Autonomy\": 40, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert assess_agent_level(f1) == \"L3-高级\"\n        assert assess_agent_level(f2) == \"L2-标准\"\n        assert assess_agent_level(f3) == \"L1-基础\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_6(student_func):\n    # L3\n    f1 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 80, \"Learning\": 75}\n    assert student_func(f1) == \"L3-高级\"\n    # L2\n    f2 = {\"Autonomy\": 80, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70, \"Memory\": 30, \"Learning\": 20}\n    assert student_func(f2) == \"L2-标准\"\n    # L1\n    f3 = {\"Autonomy\": 40, \"Perception\": 75, \"Reasoning\": 90, \"Planning\": 65, \"Action\": 70}\n    assert student_func(f3) == \"L1-基础\"\n    return True, \"关卡通过：正确评估智能体等级\""
  },
  {
    "id": "1-7",
    "title": "反应式智能体的工作原理",
    "module": "模块一 · 智能体基础通识",
    "description": "反应式智能体（Simple Reflex Agent）不维护内部状态，仅根据当前感知直接映射\n到行动。核心是\"条件-动作规则\"（Condition-Action Rule）。\n\n编写函数 `reflex_agent(percept, rules)`，实现反应式智能体：\n- percept：当前感知（字符串）\n- rules：条件-动作规则字典，如 {\"dirty\": \"Suck\", \"clean\": \"Move\"}\n- 返回对应的动作字符串，若无匹配规则返回 \"NoOp\"",
    "input_output": "输入：percept（字符串），rules（dict[str, str]）\n输出：动作字符串\n样例输入：percept=\"dirty\", rules={\"dirty\": \"Suck\", \"clean\": \"Move\"}\n样例输出：\"Suck\"",
    "skeleton_code": "# ==========你的代码开始==========\ndef reflex_agent(percept, rules):\n    \"\"\"\n    简单反应式智能体：根据当前感知查表返回动作。\n    percept: 当前感知（str）\n    rules:   条件-动作规则（dict）\n    返回:    对应的动作（str），无匹配则返回 \"NoOp\"\n    \"\"\"\n    # TODO: 实现条件-动作映射\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reflex_agent ═══\")\n    rules = {\"dirty\": \"Suck\", \"clean\": \"Move\", \"obstacle\": \"Avoid\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert reflex_agent(\"dirty\", rules) == \"Suck\"\n        assert reflex_agent(\"clean\", rules) == \"Move\"\n        assert reflex_agent(\"obstacle\", rules) == \"Avoid\"\n        assert reflex_agent(\"unknown\", rules) == \"NoOp\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reflex_agent ═══\")\n    rules = {\"dirty\": \"Suck\", \"clean\": \"Move\", \"obstacle\": \"Avoid\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert reflex_agent(\"dirty\", rules) == \"Suck\"\n        assert reflex_agent(\"clean\", rules) == \"Move\"\n        assert reflex_agent(\"obstacle\", rules) == \"Avoid\"\n        assert reflex_agent(\"unknown\", rules) == \"NoOp\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_7(student_func):\n    rules = {\"dirty\": \"Suck\", \"clean\": \"Move\", \"obstacle\": \"Avoid\"}\n    assert student_func(\"dirty\", rules) == \"Suck\"\n    assert student_func(\"clean\", rules) == \"Move\"\n    assert student_func(\"obstacle\", rules) == \"Avoid\"\n    assert student_func(\"unknown\", rules) == \"NoOp\"\n    return True, \"关卡通过：正确实现反应式智能体\""
  },
  {
    "id": "1-8",
    "title": "基于记忆与目标导向智能体的工作原理",
    "module": "模块一 · 智能体基础通识",
    "description": "目标导向智能体（Goal-based Agent）维护内部状态记忆，根据当前状态和目标选择\n最优行动。比反应式智能体更高级，能考虑未来行动的后果。\n\n编写类 `GoalBasedAgent`：\n- `__init__(self, initial_state, goal_state)`：初始化状态和目标\n- `perceive(self, observation)`：更新内部状态记忆（history 列表追加）\n- `decide(self)`：如果当前状态不等于目标状态，返回向目标靠近的行动；否则返回 \"Stay\"\n- `act(self, action)`：模拟执行行动，更新当前状态",
    "input_output": "输入：创建 agent = GoalBasedAgent(\"A\", \"C\")，调用 perceive() 和 decide()\n输出：decide() 返回下一步行动描述字符串",
    "skeleton_code": "class GoalBasedAgent:\n    def __init__(self, initial_state, goal_state):\n        self.state = initial_state\n        self.goal = goal_state\n        self.history = []\n\n    # ==========你的代码开始==========\n    def perceive(self, observation):\n        \"\"\"将观察加入历史\"\"\"\n        # TODO: 追加到 history\n        pass\n\n    def decide(self):\n        \"\"\"根据当前状态和目标决定行动\"\"\"\n        # TODO: 如果 state != goal，返回行动描述\n        pass\n\n    def act(self, action):\n        \"\"\"执行行动，更新状态\"\"\"\n        # TODO: 更新 self.state\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: GoalBasedAgent ═══\")\n    obj = GoalBasedAgent(\"initial_state_value\", \"goal_state_value\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        agent.perceive(\"at A\")\n        decision = agent.decide()\n        agent.act(\"Move to B\")\n        assert agent.state == \"A\"\n        assert len(agent.history) == 1, \"  断言失败\"\n        assert decision != \"Stay\"\n        assert agent.state != \"A\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: GoalBasedAgent ═══\")\n    obj = GoalBasedAgent(\"initial_state_value\", \"goal_state_value\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        agent.perceive(\"at A\")\n        decision = agent.decide()\n        agent.act(\"Move to B\")\n        assert agent.state == \"A\"\n        assert len(agent.history) == 1, \"  断言失败\"\n        assert decision != \"Stay\"\n        assert agent.state != \"A\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_8(module_dict):\n    GoalBasedAgent = module_dict.get(\"GoalBasedAgent\")\n    assert GoalBasedAgent is not None, \"未找到 GoalBasedAgent 类\"\n    agent = GoalBasedAgent(\"A\", \"C\")\n    agent.perceive(\"at A\")\n    assert agent.state == \"A\"\n    assert len(agent.history) == 1\n    decision = agent.decide()\n    assert decision != \"Stay\", \"状态A和目标C不同，应该行动\"\n    agent.act(\"Move to B\")\n    assert agent.state != \"A\", \"状态应已改变\"\n    return True, \"关卡通过：正确实现目标导向智能体\""
  },
  {
    "id": "1-9",
    "title": "智能体的基本运行闭环 Observe→Think→Act",
    "module": "模块一 · 智能体基础通识",
    "description": "AI 智能体的核心运行闭环：观察（Observe）→ 思考（Think）→ 行动（Act），\n简称 OTA 循环。每一步都依赖上一步的输出作为输入。\n\n编写函数 `ota_loop(environment, max_steps=5)`，模拟 OTA 循环：\n- Observe：读取环境当前状态\n- Think：根据状态生成计划（plan）\n- Act：执行计划中的行动，更新环境\n- 循环直到目标达成或达到 max_steps\n- 返回执行日志列表，每步格式：\"Step N: Observe=X → Think=Y → Act=Z\"",
    "input_output": "输入：environment（dict，包含 \"state\" 和 \"goal\"），max_steps（int）\n输出：list[str]，每步的日志记录\n样例输入：{\"state\": \"idle\", \"goal\": \"completed\"}, max_steps=3\n样例输出：[\"Step 1: Observe=idle → Think=start → Act=processing\", \"Step 2: Observe=processing → Think=finish → Act=completed\"]",
    "skeleton_code": "# ==========你的代码开始==========\ndef ota_loop(environment, max_steps=5):\n    \"\"\"\n    模拟 Observe → Think → Act 循环。\n    environment: {\"state\": ..., \"goal\": ...}\n    返回日志列表\n    \"\"\"\n    pass  # TODO: 实现此方法\n        # 1. Observe\n        # 2. Think: 根据观察和目标决定计划\n        # 3. Act: 执行计划\n        # TODO: 完善上述逻辑并生成日志字符串\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ota_loop ═══\")\n    env = {\"state\": \"idle\", \"goal\": \"completed\"}\n    env2 = {\"state\": \"done\", \"goal\": \"done\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        log = ota_loop(env, max_steps=3)\n        log2 = ota_loop(env2, max_steps=3)\n        assert len(log) >= 1\n        assert \"Observe\" in log[0] and \"Think\" in log[0] and \"Act\" in log[0], \"  断言失败\"\n        assert env.get(\"state\") == \"completed\"\n        assert len(log2) == 0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ota_loop ═══\")\n    env = {\"state\": \"idle\", \"goal\": \"completed\"}\n    env2 = {\"state\": \"done\", \"goal\": \"done\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        log = ota_loop(env, max_steps=3)\n        log2 = ota_loop(env2, max_steps=3)\n        assert len(log) >= 1\n        assert \"Observe\" in log[0] and \"Think\" in log[0] and \"Act\" in log[0], \"  断言失败\"\n        assert env.get(\"state\") == \"completed\"\n        assert len(log2) == 0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_1_9(student_func):\n    env = {\"state\": \"idle\", \"goal\": \"completed\"}\n    log = student_func(env, max_steps=3)\n    assert len(log) >= 1, \"至少应有1步日志\"\n    assert \"Observe\" in log[0] and \"Think\" in log[0] and \"Act\" in log[0]\n    assert env.get(\"state\") == \"completed\", \"最终应达成目标\"\n    # 测试已达成目标的情况\n    env2 = {\"state\": \"done\", \"goal\": \"done\"}\n    log2 = student_func(env2, max_steps=3)\n    assert len(log2) == 0, \"已达成目标不应有步骤\"\n    return True, \"关卡通过：正确实现OTA闭环\""
  },
  {
    "id": "2-1",
    "title": "Transformer 架构核心 — 自注意力机制与位置编码",
    "module": "模块二 · 大模型与提示词工程",
    "description": "Transformer 的核心是自注意力机制（Self-Attention），它让模型理解一个序列中\n每个词与其他所有词的关系。公式简化版：Attention(Q,K,V) = softmax(QK^T/√dk)V，\n其中 Q=Query, K=Key, V=Value 均由输入向量通过线性变换得到。位置编码（Positional\nEncoding）则给词向量加上位置信息，因为 Self-Attention 本身不感知顺序。\n\n请实现函数 `scaled_dot_product_attention(Q, K, V)`，用 Python 原生数学模拟注意力\n计算（不需要真实矩阵运算，用列表嵌套模拟 2x2 矩阵即可）。",
    "input_output": "输入：Q, K, V 各为一个 2x2 列表（如 [[1,0],[0,1]]）\n输出：注意力输出矩阵（2x2 列表），每个元素保留 3 位小数\n样例输入：Q=[[1,0],[0,1]], K=[[1,0],[0,1]], V=[[1,2],[3,4]]\n样例输出：[[0.731, 0.731], [2.269, 3.269]]",
    "skeleton_code": "import math\n\n# ==========你的代码开始==========\ndef softmax_row(row):\n    \"\"\"对一行做 softmax，返回新列表\"\"\"\n    pass  # TODO: 实现此方法\n\ndef scaled_dot_product_attention(Q, K, V):\n    \"\"\"\n    Q, K, V: 2x2 列表\n    计算 softmax(Q * K^T / sqrt(dk)) * V\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 1. 计算 Q * K^T; 2. 除以 sqrt(dk); 3. softmax; 4. 乘以 V\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: softmax_row ═══\")\n    Q = [[1.0, 0.0], [0.0, 1.0]]\n    K = [[1.0, 0.0], [0.0, 1.0]]\n    V = [[1.0, 2.0], [3.0, 4.0]]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = softmax_row(Q, K, V)\n        assert len(result) == 2 and len(result[0]) == 2\n        assert all(isinstance(v, float) for v in row)\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: softmax_row ═══\")\n    Q = [[1.0, 0.0], [0.0, 1.0]]\n    K = [[1.0, 0.0], [0.0, 1.0]]\n    V = [[1.0, 2.0], [3.0, 4.0]]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = softmax_row(Q, K, V)\n        assert len(result) == 2 and len(result[0]) == 2\n        assert all(isinstance(v, float) for v in row)\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_1(student_func):\n    Q = [[1.0, 0.0], [0.0, 1.0]]\n    K = [[1.0, 0.0], [0.0, 1.0]]\n    V = [[1.0, 2.0], [3.0, 4.0]]\n    result = student_func(Q, K, V)\n    assert len(result) == 2 and len(result[0]) == 2, \"输出应为2x2矩阵\"\n    # 检查每行和为 V 对应行的加权和（softmax后每行和为1）\n    for row in result:\n        assert all(isinstance(v, float) for v in row), \"元素应为浮点数\"\n    return True, \"关卡通过：正确实现注意力计算\""
  },
  {
    "id": "2-2",
    "title": "预训练与指令微调",
    "module": "模块二 · 大模型与提示词工程",
    "description": "大模型的训练分为两个关键阶段：\n1. 预训练（Pre-training）：在海量无标注文本上，用\"预测下一个词\"任务训练基座模型\n2. 指令微调（Instruction Tuning）：在有标注的指令-回答数据上微调，让模型学会遵循指令\n\n编写函数 `simulate_finetune_effect(base_model_knowledge, instruction_data)`：\n- base_model_knowledge：基座模型对词的概率分布 dict\n- instruction_data：指令微调数据，list of (question, answer) tuples\n- 模拟微调：如果 question 中的词在 knowledge 中，将其概率提高 20%\n- 返回微调后的概率分布",
    "input_output": "输入：base_model_knowledge（dict），instruction_data（list[tuple]）\n输出：微调后的概率分布（dict）\n样例输入：{\"AI\": 0.5, \"智能体\": 0.3}, [(\"什么是AI\", \"AI是人工智能\")]\n样例输出：{\"AI\": 0.6, \"智能体\": 0.3}",
    "skeleton_code": "# ==========你的代码开始==========\ndef simulate_finetune_effect(base_model_knowledge, instruction_data):\n    \"\"\"\n    模拟指令微调：对 instruction_data 中出现的词提高概率。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 遍历 instruction_data，提取词并提高概率\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_finetune_effect ═══\")\n    base = {\"AI\": 0.5, \"ML\": 0.3, \"DL\": 0.2}\n    data = [(\"什么是AI\", \"AI是人工智能\"), (\"机器学习简介\", \"ML是AI的子集\")]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = simulate_finetune_effect(base, data)\n        assert result[\"AI\"] == 0.6\n        assert result[\"ML\"] > 0.3\n        assert result[\"DL\"] == 0.2\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_finetune_effect ═══\")\n    base = {\"AI\": 0.5, \"ML\": 0.3, \"DL\": 0.2}\n    data = [(\"什么是AI\", \"AI是人工智能\"), (\"机器学习简介\", \"ML是AI的子集\")]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = simulate_finetune_effect(base, data)\n        assert result[\"AI\"] == 0.6\n        assert result[\"ML\"] > 0.3\n        assert result[\"DL\"] == 0.2\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_2(student_func):\n    base = {\"AI\": 0.5, \"ML\": 0.3, \"DL\": 0.2}\n    data = [(\"什么是AI\", \"AI是人工智能\"), (\"机器学习简介\", \"ML是AI的子集\")]\n    result = student_func(base, data)\n    assert result[\"AI\"] == 0.6, \"AI应提高20%\"\n    assert result[\"ML\"] > 0.3, \"ML应提高\"\n    assert result[\"DL\"] == 0.2, \"DL未被提及应不变\"\n    return True, \"关卡通过：正确模拟指令微调效果\""
  },
  {
    "id": "2-3",
    "title": "LoRA 低秩适配基本概念",
    "module": "模块二 · 大模型与提示词工程",
    "description": "LoRA（Low-Rank Adaptation）是一种高效微调技术。核心思想：不修改原始大模型\n权重 W，而是训练两个小矩阵 A 和 B，使 W' = W + B×A。其中 A 和 B 的秩远小于\nW 的秩，因此可训练参数大幅减少。\n\n编写函数 `apply_lora(original_weight, lora_A, lora_B, alpha=1.0)`：\n模拟 LoRA 权重更新。original_weight 是 2x2 矩阵（列表），lora_A 是 2×r 矩阵，\nlora_B 是 r×2 矩阵（r=2 简化）。返回 W + alpha * (B × A)。",
    "input_output": "输入：original_weight(2x2), lora_A(2x2), lora_B(2x2), alpha(float)\n输出：更新后的 2x2 矩阵（保留4位小数）\n样例输入：W=[[1,0],[0,1]], A=[[0.1,0],[0,0.1]], B=[[0.1,0],[0,0.1]], alpha=1.0\n样例输出：[[1.01, 0.0], [0.0, 1.01]]",
    "skeleton_code": "# ==========你的代码开始==========\ndef apply_lora(original_weight, lora_A, lora_B, alpha=1.0):\n    \"\"\"\n    模拟 LoRA: W' = W + alpha * (B @ A)\n    original_weight, lora_A, lora_B 均为 2x2 列表\n    \"\"\"\n    # 矩阵乘法 B @ A: (2x2) x (2x2) = (2x2)\n    pass  # TODO: 实现此方法\n    # TODO: 计算 BA = B * A，然后 W' = W + alpha * BA\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: apply_lora ═══\")\n    W = [[1.0, 0.0], [0.0, 1.0]]\n    A = [[0.1, 0.0], [0.0, 0.1]]\n    B = [[0.1, 0.0], [0.0, 0.1]]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = apply_lora(W, A, B, 1.0)\n        assert abs(result[0][0] - 1.01) < 0.001\n        assert abs(result[1][1] - 1.01) < 0.001\n        assert abs(result[0][1] - 0.0) < 0.001\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: apply_lora ═══\")\n    W = [[1.0, 0.0], [0.0, 1.0]]\n    A = [[0.1, 0.0], [0.0, 0.1]]\n    B = [[0.1, 0.0], [0.0, 0.1]]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = apply_lora(W, A, B, 1.0)\n        assert abs(result[0][0] - 1.01) < 0.001\n        assert abs(result[1][1] - 1.01) < 0.001\n        assert abs(result[0][1] - 0.0) < 0.001\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_3(student_func):\n    W = [[1.0, 0.0], [0.0, 1.0]]\n    A = [[0.1, 0.0], [0.0, 0.1]]\n    B = [[0.1, 0.0], [0.0, 0.1]]\n    result = student_func(W, A, B, 1.0)\n    assert abs(result[0][0] - 1.01) < 0.001, \"位置(0,0)应为1.01\"\n    assert abs(result[1][1] - 1.01) < 0.001, \"位置(1,1)应为1.01\"\n    assert abs(result[0][1] - 0.0) < 0.001, \"非对角线应不变\"\n    return True, \"关卡通过：正确实现LoRA低秩适配\""
  },
  {
    "id": "2-4",
    "title": "上下文学习与少样本提示",
    "module": "模块二 · 大模型与提示词工程",
    "description": "上下文学习（In-Context Learning）是大模型的涌现能力：在 Prompt 中给几个示例\n（Few-shot），模型就能理解任务格式并正确回答，无需参数更新。\n\n编写函数 `build_few_shot_prompt(task, examples, query)`：\n- task：任务描述字符串\n- examples：示例列表 [(输入, 输出), ...]\n- query：用户实际要问的输入\n- 返回拼接好的完整 Prompt 字符串，格式：\n  [任务描述]\n  示例1: 输入→输出\n  示例2: 输入→输出\n  现在请回答: query",
    "input_output": "输入：task(str), examples(list[tuple]), query(str)\n输出：格式化后的 Prompt 字符串",
    "skeleton_code": "# ==========你的代码开始==========\ndef build_few_shot_prompt(task, examples, query):\n    \"\"\"\n    构建 Few-shot 提示词。\n    \"\"\"\n    # TODO: 按格式拼接 task, examples, query\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: build_few_shot_prompt ═══\")\n    task = \"将英文翻译成中文\"\n    examples = [(\"hello\", \"你好\"), (\"world\", \"世界\")]\n    query = \"AI agent\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = build_few_shot_prompt(task, examples, query)\n        assert task in result\n        assert \"hello\" in result and \"你好\" in result\n        assert \"world\" in result and \"世界\" in result\n        assert \"AI agent\" in result\n        assert result.index(task) < result.index(\"hello\")\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: build_few_shot_prompt ═══\")\n    task = \"将英文翻译成中文\"\n    examples = [(\"hello\", \"你好\"), (\"world\", \"世界\")]\n    query = \"AI agent\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = build_few_shot_prompt(task, examples, query)\n        assert task in result\n        assert \"hello\" in result and \"你好\" in result\n        assert \"world\" in result and \"世界\" in result\n        assert \"AI agent\" in result\n        assert result.index(task) < result.index(\"hello\")\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_4(student_func):\n    task = \"将英文翻译成中文\"\n    examples = [(\"hello\", \"你好\"), (\"world\", \"世界\")]\n    query = \"AI agent\"\n    result = student_func(task, examples, query)\n    assert task in result, \"应包含任务描述\"\n    assert \"hello\" in result and \"你好\" in result, \"应包含示例1\"\n    assert \"world\" in result and \"世界\" in result, \"应包含示例2\"\n    assert \"AI agent\" in result, \"应包含查询\"\n    assert result.index(task) < result.index(\"hello\"), \"任务描述应在示例之前\"\n    return True, \"关卡通过：正确构建Few-shot提示词\""
  },
  {
    "id": "2-5",
    "title": "Prompt 标准结构",
    "module": "模块二 · 大模型与提示词工程",
    "description": "标准 Prompt 由三部分组成：\n1. 系统指令（System Prompt）：定义角色和能力\n2. 用户指令（User Prompt）：具体任务要求\n3. 输出格式限定（Format Spec）：指定输出 JSON/Markdown/纯文本等\n\n编写函数 `build_structured_prompt(role, task, output_format)`，返回一个字典，\n包含 system、user、format 三个键，并模拟生成最终发给 LLM 的完整消息列表。",
    "input_output": "输入：role(str), task(str), output_format(str)\n输出：dict，包含 \"messages\" 列表（每条为 {\"role\":..., \"content\":...}）",
    "skeleton_code": "# ==========你的代码开始==========\ndef build_structured_prompt(role, task, output_format):\n    \"\"\"\n    构建标准 Prompt 结构，返回包含 messages 的字典。\n    \"\"\"\n    # TODO: 构建 system + user 消息列表\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: build_structured_prompt ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = build_structured_prompt(\"Python专家\", \"写一个排序函数\", \"Python代码\")\n        assert len(msgs) == 2\n        assert msgs[0][\"role\"] == \"system\"\n        assert msgs[1][\"role\"] == \"user\"\n        assert \"Python专家\" in msgs[0][\"content\"]\n        assert \"排序函数\" in msgs[1][\"content\"]\n        assert \"Python代码\" in msgs[1][\"content\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: build_structured_prompt ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = build_structured_prompt(\"Python专家\", \"写一个排序函数\", \"Python代码\")\n        assert len(msgs) == 2\n        assert msgs[0][\"role\"] == \"system\"\n        assert msgs[1][\"role\"] == \"user\"\n        assert \"Python专家\" in msgs[0][\"content\"]\n        assert \"排序函数\" in msgs[1][\"content\"]\n        assert \"Python代码\" in msgs[1][\"content\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_5(student_func):\n    result = student_func(\"Python专家\", \"写一个排序函数\", \"Python代码\")\n    msgs = result.get(\"messages\", [])\n    assert len(msgs) == 2, \"应包含system和user两条消息\"\n    assert msgs[0][\"role\"] == \"system\", \"第一条应为system\"\n    assert msgs[1][\"role\"] == \"user\", \"第二条应为user\"\n    assert \"Python专家\" in msgs[0][\"content\"], \"system应包含角色\"\n    assert \"排序函数\" in msgs[1][\"content\"], \"user应包含任务\"\n    assert \"Python代码\" in msgs[1][\"content\"], \"user应包含格式限定\"\n    return True, \"关卡通过：正确构建标准Prompt结构\""
  },
  {
    "id": "2-6",
    "title": "思维链 Chain of Thought",
    "module": "模块二 · 大模型与提示词工程",
    "description": "思维链（Chain of Thought, CoT）通过在 Prompt 中加入\"让我们一步一步思考\"\n（Let's think step by step），引导模型将复杂问题分解为中间推理步骤。\n\n编写函数 `cot_solver(problem, steps)`：\n- problem：数学文字题字符串\n- steps：推理步骤数\n- 模拟 CoT 流程：每步输出中间结果，最终返回答案\n- 用 eval() 安全计算最终表达式（仅限基本运算 +-*/）",
    "input_output": "输入：problem(str 如 \"计算 3+5*2\"), steps(int)\n输出：{\"reasoning\": [\"步骤1: 先算乘法 5*2=10\", \"步骤2: 再算加法 3+10=13\"], \"answer\": 13}",
    "skeleton_code": "# ==========你的代码开始==========\ndef cot_solver(problem, steps=2):\n    \"\"\"\n    模拟思维链推理。problem 格式: \"计算 a op b op c\"\n    支持 +-*/, 遵循运算优先级。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 解析表达式，按步骤拆分计算，返回推理过程和答案\n    # 简易实现：用 eval 计算，但分步展示\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: cot_solver ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = cot_solver(\"计算 3+5*2\", 2)\n        result2 = cot_solver(\"计算 10/2+3\", 2)\n        assert result[\"answer\"] == 13\n        assert len(result[\"reasoning\"]) >= 1\n        assert result2[\"answer\"] == 8.0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: cot_solver ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = cot_solver(\"计算 3+5*2\", 2)\n        result2 = cot_solver(\"计算 10/2+3\", 2)\n        assert result[\"answer\"] == 13\n        assert len(result[\"reasoning\"]) >= 1\n        assert result2[\"answer\"] == 8.0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_6(student_func):\n    result = student_func(\"计算 3+5*2\", 2)\n    assert result[\"answer\"] == 13, \"3+5*2应等于13\"\n    assert len(result[\"reasoning\"]) >= 1, \"至少应有推理步骤\"\n    result2 = student_func(\"计算 10/2+3\", 2)\n    assert result2[\"answer\"] == 8.0, \"10/2+3应等于8\"\n    return True, \"关卡通过：正确实现思维链推理\""
  },
  {
    "id": "2-7",
    "title": "思维树 Tree of Thoughts",
    "module": "模块二 · 大模型与提示词工程",
    "description": "思维树（Tree of Thoughts, ToT）是 CoT 的升级版：同时探索多条推理路径，\n评估每条路径的质量，选择最优路径继续。类似\"广度优先搜索+剪枝\"。\n\n编写函数 `tot_search(branches, scores)` 模拟 ToT 路径选择：\n- branches：每条路径的分数列表\n- 选择得分最高的前 k 条路径（k=min(3, len(branches))）\n- 如果最高分路径有多个并列，全部保留\n- 返回入选路径的索引列表",
    "input_output": "输入：branches(list[float]), scores(list[float])\n输出：top_k_indices(list[int])",
    "skeleton_code": "# ==========你的代码开始==========\ndef tot_search(branches_scores):\n    \"\"\"\n    branches_scores: list of (branch_name, score) tuples\n    返回得分最高的前k条路径的 branch_name 列表\n    \"\"\"\n    # TODO: 按分数排序，取前 k=3，包括所有并列最高分\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: tot_search ═══\")\n    bs = [(\"A\", 0.8), (\"B\", 0.5), (\"C\", 0.9), (\"D\", 0.7)]\n    bs2 = [(\"X\", 0.9), (\"Y\", 0.9), (\"Z\", 0.9), (\"W\", 0.5)]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = tot_search(bs)\n        result2 = tot_search(bs2)\n        assert len(result) <= 3\n        assert result[0] == \"C\"\n        assert \"A\" in result\n        assert len(result2) == 3\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: tot_search ═══\")\n    bs = [(\"A\", 0.8), (\"B\", 0.5), (\"C\", 0.9), (\"D\", 0.7)]\n    bs2 = [(\"X\", 0.9), (\"Y\", 0.9), (\"Z\", 0.9), (\"W\", 0.5)]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = tot_search(bs)\n        result2 = tot_search(bs2)\n        assert len(result) <= 3\n        assert result[0] == \"C\"\n        assert \"A\" in result\n        assert len(result2) == 3\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_7(student_func):\n    bs = [(\"A\", 0.8), (\"B\", 0.5), (\"C\", 0.9), (\"D\", 0.7)]\n    result = student_func(bs)\n    assert len(result) <= 3, \"至多保留3条\"\n    assert result[0] == \"C\", \"最高分C应排第一\"\n    assert \"A\" in result, \"0.8分应入选\"\n    # 测试并列\n    bs2 = [(\"X\", 0.9), (\"Y\", 0.9), (\"Z\", 0.9), (\"W\", 0.5)]\n    result2 = student_func(bs2)\n    assert len(result2) == 3, \"三个并列最高分应全部保留\"\n    return True, \"关卡通过：正确实现思维树搜索\""
  },
  {
    "id": "2-8",
    "title": "ReAct 模式 — 推理与行动交替",
    "module": "模块二 · 大模型与提示词工程",
    "description": "ReAct（Reasoning + Acting）模式让智能体交替进行推理和行动：先思考下一步该\n做什么（Reasoning），然后执行具体操作（Acting），再根据结果继续推理。\n\n编写函数 `react_loop(task, tools, max_rounds=5)` 模拟 ReAct 循环：\n- 每轮先输出推理（Thought），再执行行动（Action），最后观察结果（Observation）\n- tools 字典：{工具名: lambda input: output}\n- 任务示例：计算 \"(3+5)*2\"，tools={\"calc\": lambda x: eval(x)}\n- 返回每一轮的日志列表",
    "input_output": "输入：task(str), tools(dict), max_rounds(int)\n输出：list[dict]，每轮 {\"Thought\":..., \"Action\":..., \"Observation\":...}",
    "skeleton_code": "# ==========你的代码开始==========\ndef react_loop(task, tools, max_rounds=5):\n    \"\"\"\n    模拟 ReAct 交替推理与行动。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 实现 Thought → Action → Observation 循环\n        # 选择第一个可用工具\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: react_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        log = react_loop(\"(3+5)*2\", {\"calc\": lambda x: str(eval(x))}, max_rounds=2)\n        assert len(log) == 2\n        assert \"Thought\" in log[0] and \"Action\" in log[0] and \"Observation\" in log[0], \"  断言失败\"\n        assert \"16\" in log[0][\"Observation\"] or \"16\" in log[1][\"Observation\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: react_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        log = react_loop(\"(3+5)*2\", {\"calc\": lambda x: str(eval(x))}, max_rounds=2)\n        assert len(log) == 2\n        assert \"Thought\" in log[0] and \"Action\" in log[0] and \"Observation\" in log[0], \"  断言失败\"\n        assert \"16\" in log[0][\"Observation\"] or \"16\" in log[1][\"Observation\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_8(student_func):\n    log = student_func(\"(3+5)*2\", {\"calc\": lambda x: str(eval(x))}, max_rounds=2)\n    assert len(log) == 2, \"应执行2轮\"\n    assert \"Thought\" in log[0] and \"Action\" in log[0] and \"Observation\" in log[0]\n    assert \"16\" in log[0][\"Observation\"] or \"16\" in log[1][\"Observation\"], \"计算结果应为16\"\n    return True, \"关卡通过：正确实现ReAct模式\""
  },
  {
    "id": "2-9",
    "title": "模型量化与推理加速",
    "module": "模块二 · 大模型与提示词工程",
    "description": "模型量化（Quantization）通过降低模型权重的数值精度来减少显存占用和加速推理。\n常见精度：FP32（32位）→ FP16（16位）→ INT8（8位）→ INT4（4位）。\n\n编写函数 `simulate_quantization(weights, bits)`：\n- weights：浮点数列表\n- bits：目标位数（32, 16, 8, 4）\n- 模拟量化：计算缩放因子 scale = max(|w|) / (2^(bits-1)-1)，将每个权重\n  四舍五入到最近的量化级别，再反量化回浮点\n- 返回 {\"quantized\": [...], \"dequantized\": [...], \"error\": 平均绝对误差}",
    "input_output": "输入：weights(list[float]), bits(int)\n输出：dict 包含量化值、反量化值、平均误差",
    "skeleton_code": "# ==========你的代码开始==========\ndef simulate_quantization(weights, bits=8):\n    \"\"\"\n    模拟权重量化与反量化过程。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 量化（round(w/scale)*scale），计算误差\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_quantization ═══\")\n    w = [0.5, -0.3, 0.8, -0.1]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r8 = simulate_quantization(w, 8)\n        r4 = simulate_quantization(w, 4)\n        assert \"error\" in r8, \"  断言失败\"\n        assert r8[\"error\"] < 0.1\n        assert r4[\"error\"] >= r8[\"error\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_quantization ═══\")\n    w = [0.5, -0.3, 0.8, -0.1]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r8 = simulate_quantization(w, 8)\n        r4 = simulate_quantization(w, 4)\n        assert \"error\" in r8, \"  断言失败\"\n        assert r8[\"error\"] < 0.1\n        assert r4[\"error\"] >= r8[\"error\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_9(student_func):\n    w = [0.5, -0.3, 0.8, -0.1]\n    r8 = student_func(w, 8)\n    assert \"error\" in r8\n    assert r8[\"error\"] < 0.1, \"8位量化误差应较小\"\n    r4 = student_func(w, 4)\n    assert r4[\"error\"] >= r8[\"error\"], \"4位量化误差应更大\"\n    return True, \"关卡通过：正确模拟量化过程\""
  },
  {
    "id": "2-10",
    "title": "模型评估基础",
    "module": "模块二 · 大模型与提示词工程",
    "description": "模型评估的常用指标：\n- 准确率（Accuracy）：(TP+TN) / Total\n- 精确率（Precision）：TP / (TP+FP)\n- 召回率（Recall）：TP / (TP+FN)\n- F1 分数：2 * Precision * Recall / (Precision + Recall)\n\n编写函数 `calculate_metrics(tp, fp, tn, fn)`，返回上述四个指标。",
    "input_output": "输入：tp, fp, tn, fn（均为 int）\n输出：{\"accuracy\": float, \"precision\": float, \"recall\": float, \"f1\": float}",
    "skeleton_code": "# ==========你的代码开始==========\ndef calculate_metrics(tp, fp, tn, fn):\n    \"\"\"\n    计算分类模型的四个核心评估指标。\n    \"\"\"\n    # TODO: 实现 accuracy, precision, recall, f1 的计算\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: calculate_metrics ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = calculate_metrics(tp=8, fp=2, tn=85, fn=5)\n        r2 = calculate_metrics(tp=10, fp=0, tn=10, fn=0)\n        assert abs(r[\"accuracy\"] - 0.93) < 0.01\n        assert abs(r[\"precision\"] - 0.8) < 0.01\n        assert abs(r[\"recall\"] - 0.615) < 0.05\n        assert r[\"f1\"] > 0.6 and r[\"f1\"] < 0.8\n        assert r2[\"accuracy\"] == 1.0 and r2[\"precision\"] == 1.0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: calculate_metrics ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = calculate_metrics(tp=8, fp=2, tn=85, fn=5)\n        r2 = calculate_metrics(tp=10, fp=0, tn=10, fn=0)\n        assert abs(r[\"accuracy\"] - 0.93) < 0.01\n        assert abs(r[\"precision\"] - 0.8) < 0.01\n        assert abs(r[\"recall\"] - 0.615) < 0.05\n        assert r[\"f1\"] > 0.6 and r[\"f1\"] < 0.8\n        assert r2[\"accuracy\"] == 1.0 and r2[\"precision\"] == 1.0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_2_10(student_func):\n    r = student_func(tp=8, fp=2, tn=85, fn=5)\n    assert abs(r[\"accuracy\"] - 0.93) < 0.01, f\"accuracy应为0.93, 得到{r['accuracy']}\"\n    assert abs(r[\"precision\"] - 0.8) < 0.01, f\"precision应为0.8\"\n    assert abs(r[\"recall\"] - 0.615) < 0.05, f\"recall应≈0.615\"\n    assert r[\"f1\"] > 0.6 and r[\"f1\"] < 0.8, \"f1应在合理范围\"\n    # 边界：全对\n    r2 = student_func(tp=10, fp=0, tn=10, fn=0)\n    assert r2[\"accuracy\"] == 1.0 and r2[\"precision\"] == 1.0\n    return True, \"关卡通过：正确计算模型评估指标\""
  },
  {
    "id": "3-1",
    "title": "自然语言指令解析",
    "module": "模块三 · 智能体四大核心能力模块 — 感知模块",
    "description": "智能体感知模块的第一个任务：精准解析用户的自然语言指令，提取意图（intent）\n和关键实体（entities）。例如 \"帮我查询明天北京的天气\" → intent=\"query_weather\",\nentities={\"date\":\"明天\",\"city\":\"北京\"}。\n\n编写函数 `parse_nl_instruction(text)`，使用关键词匹配（不依赖外部库）：\n- 如果包含\"天气\"→ intent=\"query_weather\"，提取城市名和日期\n- 如果包含\"搜索\"或\"查找\"→ intent=\"search\"，提取搜索内容\n- 如果包含\"计算\"→ intent=\"calculate\"，提取表达式\n- 否则 intent=\"unknown\"",
    "input_output": "输入：text（str）\n输出：{\"intent\": str, \"entities\": dict}",
    "skeleton_code": "# ==========你的代码开始==========\ndef parse_nl_instruction(text):\n    \"\"\"\n    解析自然语言指令，提取意图和实体。\n    \"\"\"\n    # TODO: 关键词匹配提取 intent 和 entities\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parse_nl_instruction ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = parse_nl_instruction(\"帮我查询明天北京的天气\")\n        r2 = parse_nl_instruction(\"搜索AI智能体最新论文\")\n        r3 = parse_nl_instruction(\"计算 3+5*2\")\n        r4 = parse_nl_instruction(\"今天心情不错\")\n        assert r1[\"intent\"] == \"query_weather\"\n        assert \"北京\" in str(r1[\"entities\"])\n        assert r2[\"intent\"] == \"search\"\n        assert r3[\"intent\"] == \"calculate\"\n        assert r4[\"intent\"] == \"unknown\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parse_nl_instruction ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = parse_nl_instruction(\"帮我查询明天北京的天气\")\n        r2 = parse_nl_instruction(\"搜索AI智能体最新论文\")\n        r3 = parse_nl_instruction(\"计算 3+5*2\")\n        r4 = parse_nl_instruction(\"今天心情不错\")\n        assert r1[\"intent\"] == \"query_weather\"\n        assert \"北京\" in str(r1[\"entities\"])\n        assert r2[\"intent\"] == \"search\"\n        assert r3[\"intent\"] == \"calculate\"\n        assert r4[\"intent\"] == \"unknown\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_1(student_func):\n    r1 = student_func(\"帮我查询明天北京的天气\")\n    assert r1[\"intent\"] == \"query_weather\", f\"应为query_weather, 得到{r1['intent']}\"\n    assert \"北京\" in str(r1[\"entities\"]), \"应提取城市北京\"\n    r2 = student_func(\"搜索AI智能体最新论文\")\n    assert r2[\"intent\"] == \"search\"\n    r3 = student_func(\"计算 3+5*2\")\n    assert r3[\"intent\"] == \"calculate\"\n    r4 = student_func(\"今天心情不错\")\n    assert r4[\"intent\"] == \"unknown\"\n    return True, \"关卡通过：正确解析自然语言指令\""
  },
  {
    "id": "3-2",
    "title": "多模态感知模拟",
    "module": "模块三 · 智能体四大核心能力模块 — 感知模块",
    "description": "多模态感知指智能体能同时处理文本、图像、语音等多种输入类型，并统一理解为\n结构化的语义表示。本关模拟多模态输入的融合处理。\n\n编写函数 `fuse_multimodal_inputs(inputs)`：\n- inputs：dict，可能包含 \"text\"（str）, \"image_desc\"（str）, \"audio_transcript\"（str）\n- 融合所有可用模态的信息，返回统一的语义摘要字符串\n- 格式：\"[融合感知] 文本: ... 图像描述: ... 语音转录: ...\"",
    "input_output": "输入：inputs(dict)\n输出：融合后的摘要字符串",
    "skeleton_code": "# ==========你的代码开始==========\ndef fuse_multimodal_inputs(inputs):\n    \"\"\"\n    融合多模态输入为统一语义表示。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 遍历 inputs，拼接各模态信息\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: fuse_multimodal_inputs ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = fuse_multimodal_inputs({\"text\": \"一只猫\", \"image_desc\": \"橙色虎斑猫\", \"audio_transcript\": \"喵喵叫\"})\n        r2 = fuse_multimodal_inputs({\"text\": \"只有文本\"})\n        assert \"text\" in r and \"image_desc\" in r and \"audio_transcript\" in r, \"  断言失败\"\n        assert \"一只猫\" in r and \"橙色虎斑猫\" in r, \"  断言失败\"\n        assert \"image_desc\" not in r2.lower(), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: fuse_multimodal_inputs ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = fuse_multimodal_inputs({\"text\": \"一只猫\", \"image_desc\": \"橙色虎斑猫\", \"audio_transcript\": \"喵喵叫\"})\n        r2 = fuse_multimodal_inputs({\"text\": \"只有文本\"})\n        assert \"text\" in r and \"image_desc\" in r and \"audio_transcript\" in r, \"  断言失败\"\n        assert \"一只猫\" in r and \"橙色虎斑猫\" in r, \"  断言失败\"\n        assert \"image_desc\" not in r2.lower(), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_2(student_func):\n    r = student_func({\"text\": \"一只猫\", \"image_desc\": \"橙色虎斑猫\", \"audio_transcript\": \"喵喵叫\"})\n    assert \"text\" in r and \"image_desc\" in r and \"audio_transcript\" in r\n    assert \"一只猫\" in r and \"橙色虎斑猫\" in r\n    r2 = student_func({\"text\": \"只有文本\"})\n    assert \"image_desc\" not in r2.lower()\n    return True, \"关卡通过：正确融合多模态输入\""
  },
  {
    "id": "3-3",
    "title": "视觉语言模型（VLM）的基本作用",
    "module": "模块三 · 智能体四大核心能力模块 — 感知模块",
    "description": "VLM（Vision Language Model）能理解图像内容并生成文本描述。本关模拟 VLM 的\n图像→文本映射过程。\n\n编写函数 `simulate_vlm(image_features, concept_map)`：\n- image_features：图像特征词列表 [\"furry\",\"pointy_ears\",\"whiskers\"]\n- concept_map：特征→概念的映射字典 {\"furry\":\"动物\",\"pointy_ears\":\"猫科\",\"whiskers\":\"猫\"}\n- 返回最可能的概念（出现次数最多的类别）及置信度",
    "input_output": "输入：image_features(list[str]), concept_map(dict[str,str])\n输出：{\"concept\": str, \"confidence\": float}  (0~1)\n样例输入：features=[\"furry\",\"whiskers\"], map={\"furry\":\"猫\",\"whiskers\":\"猫\",\"pointy_ears\":\"狗\"}\n样例输出：{\"concept\": \"猫\", \"confidence\": 1.0}",
    "skeleton_code": "# ==========你的代码开始==========\ndef simulate_vlm(image_features, concept_map):\n    \"\"\"\n    模拟VLM：根据图像特征匹配最可能的概念。\n    \"\"\"\n    # TODO: 统计每个概念的出现次数，返回最高频概念和占比\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_vlm ═══\")\n    concept_map = {\"furry\": \"猫\", \"whiskers\": \"猫\", \"pointy_ears\": \"狗\", \"tail\": \"猫\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = simulate_vlm([\"furry\", \"whiskers\"], concept_map)\n        r2 = simulate_vlm([\"furry\", \"pointy_ears\"], concept_map)\n        assert r[\"concept\"] == \"猫\"\n        assert r[\"confidence\"] == 1.0\n        assert r2[\"concept\"] in [\"猫\", \"狗\"], \"  断言失败\"\n        assert r2[\"confidence\"] == 0.5\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: simulate_vlm ═══\")\n    concept_map = {\"furry\": \"猫\", \"whiskers\": \"猫\", \"pointy_ears\": \"狗\", \"tail\": \"猫\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = simulate_vlm([\"furry\", \"whiskers\"], concept_map)\n        r2 = simulate_vlm([\"furry\", \"pointy_ears\"], concept_map)\n        assert r[\"concept\"] == \"猫\"\n        assert r[\"confidence\"] == 1.0\n        assert r2[\"concept\"] in [\"猫\", \"狗\"], \"  断言失败\"\n        assert r2[\"confidence\"] == 0.5\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_3(student_func):\n    concept_map = {\"furry\": \"猫\", \"whiskers\": \"猫\", \"pointy_ears\": \"狗\", \"tail\": \"猫\"}\n    r = student_func([\"furry\", \"whiskers\"], concept_map)\n    assert r[\"concept\"] == \"猫\", \"应识别为猫\"\n    assert r[\"confidence\"] == 1.0, \"全部特征指向猫，置信度应为1.0\"\n    r2 = student_func([\"furry\", \"pointy_ears\"], concept_map)\n    assert r2[\"concept\"] in [\"猫\", \"狗\"]\n    assert r2[\"confidence\"] == 0.5, \"各一半特征，置信度应为0.5\"\n    return True, \"关卡通过：正确模拟VLM概念识别\""
  },
  {
    "id": "3-4",
    "title": "短期记忆 — 上下文窗口管理",
    "module": "模块三 · 智能体四大核心能力模块 — 记忆模块",
    "description": "大模型的上下文窗口有长度限制。短期记忆管理需要维护最近 N 条对话，超出限制时\n自动遗忘最早的对话。这类似于一个固定大小的\"滑动窗口\"队列。\n\n编写类 `ShortTermMemory`：\n- `__init__(self, max_size=5)`：设置最大容量\n- `add(self, message)`：添加一条消息（dict），超出容量时移除最早的\n- `get_context(self)`：返回当前所有消息列表\n- `get_token_estimate(self)`：估算总 token 数（按中文字符数 / 2 估算）",
    "input_output": "创建 memory = ShortTermMemory(3)，依次 add(\"msg1\"), add(\"msg2\"), add(\"msg3\"), add(\"msg4\")\nget_context() 返回 [\"msg2\",\"msg3\",\"msg4\"]",
    "skeleton_code": "class ShortTermMemory:\n    def __init__(self, max_size=5):\n        self.max_size = max_size\n        self.messages = []\n\n    # ==========你的代码开始==========\n    def add(self, message):\n        \"\"\"添加消息，超出容量时移除最早的消息\"\"\"\n        # TODO: append + 检查长度\n        pass\n\n    def get_context(self):\n        \"\"\"返回当前上下文\"\"\"\n        # TODO\n        pass\n\n    def get_token_estimate(self):\n        \"\"\"估算 token 数\"\"\"\n        pass  # TODO: 实现此方法\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ShortTermMemory ═══\")\n    obj = ShortTermMemory(3)\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        mem.add(\"消息A\")\n        mem.add(\"消息B\")\n        mem.add(\"消息C\")\n        mem.add(\"消息D\")\n        ctx = mem.get_context()\n        assert len(mem.get_context()) == 3, \"  断言失败\"\n        assert len(ctx) == 3\n        assert \"消息A\" not in ctx\n        assert \"消息D\" in ctx\n        assert mem.get_token_estimate() > 0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ShortTermMemory ═══\")\n    obj = ShortTermMemory(3)\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        mem.add(\"消息A\")\n        mem.add(\"消息B\")\n        mem.add(\"消息C\")\n        mem.add(\"消息D\")\n        ctx = mem.get_context()\n        assert len(mem.get_context()) == 3, \"  断言失败\"\n        assert len(ctx) == 3\n        assert \"消息A\" not in ctx\n        assert \"消息D\" in ctx\n        assert mem.get_token_estimate() > 0\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_4(module_dict):\n    STM = module_dict.get(\"ShortTermMemory\")\n    assert STM is not None\n    mem = STM(max_size=3)\n    mem.add(\"消息A\")\n    mem.add(\"消息B\")\n    mem.add(\"消息C\")\n    assert len(mem.get_context()) == 3\n    mem.add(\"消息D\")\n    ctx = mem.get_context()\n    assert len(ctx) == 3, \"超出容量应保持3条\"\n    assert \"消息A\" not in ctx, \"最早的消息A应被遗忘\"\n    assert \"消息D\" in ctx, \"最新消息D应在\"\n    assert mem.get_token_estimate() > 0, \"token估算应大于0\"\n    return True, \"关卡通过：正确实现短期记忆管理\""
  },
  {
    "id": "3-5",
    "title": "长期记忆 — 向量数据库语义检索模拟",
    "module": "模块三 · 智能体四大核心能力模块 — 记忆模块",
    "description": "长期记忆使用向量数据库（如 Chroma）存储知识的向量表示，通过余弦相似度检索\n最相关内容。本关模拟这一过程（不依赖外部库）。\n\n编写函数 `semantic_search(query_embedding, memory_store, top_k=3)`：\n- query_embedding：查询向量 list[float]\n- memory_store：list of (id, text, embedding) tuples\n- 用余弦相似度计算查询与每条记忆的相似度\n- 返回相似度最高的 top_k 条记忆的 id 和 text",
    "input_output": "输入：query_embedding(list), memory_store(list[tuple]), top_k(int)\n输出：list[dict]，如 [{\"id\":..., \"text\":..., \"score\":...}, ...]",
    "skeleton_code": "import math\n\n# ==========你的代码开始==========\ndef cosine_similarity(a, b):\n    \"\"\"计算两个向量的余弦相似度\"\"\"\n    pass  # TODO: 实现此方法\n\ndef semantic_search(query_embedding, memory_store, top_k=3):\n    \"\"\"\n    余弦相似度检索最相关的 top_k 条记忆。\n    \"\"\"\n    # TODO: 计算所有相似度，排序，返回 top_k\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: cosine_similarity ═══\")\n    query = [0.7, 0.1, 0.1]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        results = cosine_similarity(query, store, top_k=2)\n        assert len(results) == 2, \"  断言失败\"\n        assert results[0][\"id\"] == \"m1\"\n        assert results[0][\"score\"] > results[1][\"score\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: cosine_similarity ═══\")\n    query = [0.7, 0.1, 0.1]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        results = cosine_similarity(query, store, top_k=2)\n        assert len(results) == 2, \"  断言失败\"\n        assert results[0][\"id\"] == \"m1\"\n        assert results[0][\"score\"] > results[1][\"score\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_5(student_func):\n    store = [\n        (\"m1\", \"AI智能体定义\", [0.8, 0.1, 0.0]),\n        (\"m2\", \"Python教程\", [0.0, 0.9, 0.1]),\n        (\"m3\", \"机器学习基础\", [0.5, 0.3, 0.6]),\n        (\"m4\", \"深度学习\", [0.15, 0.8, 0.2]),\n    ]\n    query = [0.7, 0.1, 0.1]\n    results = student_func(query, store, top_k=2)\n    assert len(results) == 2\n    assert results[0][\"id\"] == \"m1\", \"最相关的应该是m1\"\n    assert results[0][\"score\"] > results[1][\"score\"], \"应按相似度降序\"\n    return True, \"关卡通过：正确实现语义检索\""
  },
  {
    "id": "3-6",
    "title": "知识图谱作为记忆增强手段",
    "module": "模块三 · 智能体四大核心能力模块 — 记忆模块",
    "description": "知识图谱以\"实体-关系-实体\"三元组存储知识，智能体可通过图遍历找到关联信息。\n本关模拟知识图谱的关联查询。\n\n编写类 `KnowledgeGraph` 和函数 `query_related_concepts(kg, entity, max_depth=1)`：\n- 用邻接表存储三元组\n- 查询从 entity 出发 max_depth 步内可达的所有相关实体",
    "input_output": "输入：kg(KnowledgeGraph), entity(str), max_depth(int)\n输出：相关实体集合 set[str]",
    "skeleton_code": "class KnowledgeGraph:\n    # ==========你的代码开始==========\n    def __init__(self):\n        pass  # TODO: 实现此方法\n\n    def add_triple(self, subject, relation, obj):\n        \"\"\"添加 (subject, relation, object) 三元组\"\"\"\n        pass  # TODO: 实现此方法\n\ndef query_related_concepts(kg, entity, max_depth=1):\n    \"\"\"\n    BFS 从 entity 出发，返回 max_depth 步内可达的所有实体。\n    \"\"\"\n    # TODO: BFS遍历\n    pass  # TODO: 实现此方法\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: KnowledgeGraph ═══\")\n    obj = KnowledgeGraph()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_triple(\"AI\", \"包含\", \"机器学习\")\n        obj.add_triple(\"AI\", \"包含\", \"深度学习\")\n        obj.add_triple(\"机器学习\", \"依赖\", \"数学\")\n        result = query_related_concepts(kg, \"AI\", max_depth=2)\n        assert \"机器学习\" in result and \"深度学习\" in result, \"  断言失败\"\n        assert \"数学\" in result\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: KnowledgeGraph ═══\")\n    obj = KnowledgeGraph()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_triple(\"AI\", \"包含\", \"机器学习\")\n        obj.add_triple(\"AI\", \"包含\", \"深度学习\")\n        obj.add_triple(\"机器学习\", \"依赖\", \"数学\")\n        result = query_related_concepts(kg, \"AI\", max_depth=2)\n        assert \"机器学习\" in result and \"深度学习\" in result, \"  断言失败\"\n        assert \"数学\" in result\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_6(module_dict):\n    KG = module_dict.get(\"KnowledgeGraph\")\n    query_fn = module_dict.get(\"query_related_concepts\")\n    assert KG and query_fn\n    kg = KG()\n    kg.add_triple(\"AI\", \"包含\", \"机器学习\")\n    kg.add_triple(\"AI\", \"包含\", \"深度学习\")\n    kg.add_triple(\"机器学习\", \"依赖\", \"数学\")\n    result = query_fn(kg, \"AI\", max_depth=2)\n    assert \"机器学习\" in result and \"深度学习\" in result\n    assert \"数学\" in result, \"深度2应到达数学\"\n    return True, \"关卡通过：正确实现知识图谱关联查询\""
  },
  {
    "id": "3-7",
    "title": "复杂任务自动分解",
    "module": "模块三 · 智能体四大核心能力模块 — 规划模块",
    "description": "规划模块的核心能力：将复杂目标自动分解为可执行的子任务序列。\n例如 \"开发一个网页爬虫\" → [设计URL提取逻辑, 实现HTTP请求, 解析HTML, 存储数据]。\n\n编写函数 `decompose_task(goal, subtask_templates)`：\n- goal：复杂目标字符串\n- subtask_templates：关键词→子任务列表的映射\n- 匹配 goal 中的关键词，展开对应的子任务\n- 返回分解后的子任务列表（按模板顺序、去重）",
    "input_output": "输入：goal(str), subtask_templates(dict[str, list[str]])\n输出：子任务列表 list[str]",
    "skeleton_code": "# ==========你的代码开始==========\ndef decompose_task(goal, subtask_templates):\n    \"\"\"\n    根据目标中的关键词匹配子任务模板，返回分解结果。\n    \"\"\"\n    # TODO: 关键词匹配收集子任务\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: decompose_task ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = decompose_task(\"开发一个网页爬虫\", templates)\n        r2 = decompose_task(\"开发一个爬虫API\", templates)\n        assert len(r) == 4\n        assert \"实现HTTP请求\" in r, \"  断言失败\"\n        assert len(r2) == 7\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: decompose_task ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = decompose_task(\"开发一个网页爬虫\", templates)\n        r2 = decompose_task(\"开发一个爬虫API\", templates)\n        assert len(r) == 4\n        assert \"实现HTTP请求\" in r, \"  断言失败\"\n        assert len(r2) == 7\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_7(student_func):\n    templates = {\n        \"爬虫\": [\"设计URL提取逻辑\", \"实现HTTP请求\", \"解析HTML\", \"存储数据\"],\n        \"数据分析\": [\"数据清洗\", \"特征工程\", \"模型训练\", \"结果可视化\"],\n        \"API\": [\"设计接口\", \"实现路由\", \"编写文档\"],\n    }\n    r = student_func(\"开发一个网页爬虫\", templates)\n    assert len(r) == 4, \"爬虫应分解为4个子任务\"\n    assert \"实现HTTP请求\" in r\n    r2 = student_func(\"开发一个爬虫API\", templates)\n    assert len(r2) == 7, \"爬虫+API应拆分为7个\"\n    return True, \"关卡通过：正确分解复杂任务\""
  },
  {
    "id": "3-8",
    "title": "状态空间搜索与分层规划",
    "module": "模块三 · 智能体四大核心能力模块 — 规划模块",
    "description": "分层规划（Hierarchical Planning）把大问题分解为高层计划，每个高层计划再分解\n为具体步骤。本关模拟简单的路径规划问题。\n\n编写函数 `hierarchical_path_plan(start, goal, waypoints)`：\n- 将完整路径分解为\"高层路径\"（经过哪些关键节点）和\"细节步骤\"\n- start=\"A\", goal=\"D\", waypoints=[\"B\",\"C\"]\n- 返回 {\"high_level\": [\"A→B\", \"B→C\", \"C→D\"], \"steps\": [\"A→B: 直行\", \"B→C: 左转\", \"C→D: 右转\"]}",
    "input_output": "输入：start(str), goal(str), waypoints(list[str])\n输出：{\"high_level\": [...], \"steps\": [...]}",
    "skeleton_code": "# ==========你的代码开始==========\ndef hierarchical_path_plan(start, goal, waypoints):\n    \"\"\"\n    分层规划：高层经过关键节点，细节步骤逐段展开。\n    \"\"\"\n    # TODO: 构建高层路径和细节步骤\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: hierarchical_path_plan ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = hierarchical_path_plan(\"A\", \"D\", [\"B\", \"C\"])\n        r2 = hierarchical_path_plan(\"Home\", \"Office\", [])\n        assert len(r[\"high_level\"]) == 3, \"  断言失败\"\n        assert r[\"high_level\"][0] == \"A→B\"\n        assert r[\"high_level\"][2] == \"C→D\"\n        assert len(r[\"steps\"]) == 3, \"  断言失败\"\n        assert r2[\"high_level\"] == [\"Home→Office\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: hierarchical_path_plan ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = hierarchical_path_plan(\"A\", \"D\", [\"B\", \"C\"])\n        r2 = hierarchical_path_plan(\"Home\", \"Office\", [])\n        assert len(r[\"high_level\"]) == 3, \"  断言失败\"\n        assert r[\"high_level\"][0] == \"A→B\"\n        assert r[\"high_level\"][2] == \"C→D\"\n        assert len(r[\"steps\"]) == 3, \"  断言失败\"\n        assert r2[\"high_level\"] == [\"Home→Office\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_8(student_func):\n    r = student_func(\"A\", \"D\", [\"B\", \"C\"])\n    assert len(r[\"high_level\"]) == 3\n    assert r[\"high_level\"][0] == \"A→B\"\n    assert r[\"high_level\"][2] == \"C→D\"\n    assert len(r[\"steps\"]) == 3\n    r2 = student_func(\"Home\", \"Office\", [])\n    assert r2[\"high_level\"] == [\"Home→Office\"]\n    return True, \"关卡通过：正确实现分层规划\""
  },
  {
    "id": "3-9",
    "title": "根据反馈动态调整子目标",
    "module": "模块三 · 智能体四大核心能力模块 — 规划模块",
    "description": "Re-planning 是规划模块的关键能力：执行过程中遇到障碍时，动态调整剩余计划。\n\n编写函数 `replan_on_failure(original_plan, failed_step, alternative_steps)`：\n- 找到 original_plan 中 failed_step 的位置\n- 将其替换为 alternative_steps\n- 返回调整后的完整计划",
    "input_output": "输入：original_plan(list), failed_step(str), alternative_steps(list)\n输出：调整后的计划 list",
    "skeleton_code": "# ==========你的代码开始==========\ndef replan_on_failure(original_plan, failed_step, alternative_steps):\n    \"\"\"\n    计划中某一步失败时，用替代步骤替换并返回新计划。\n    \"\"\"\n    # TODO: 查找失败步骤位置，替换\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: replan_on_failure ═══\")\n    plan = [\"步骤1\", \"步骤2\", \"步骤3\", \"步骤4\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = replan_on_failure(plan, \"步骤2\", [\"替代A\", \"替代B\"])\n        r2 = replan_on_failure(plan, \"步骤4\", [\"替代X\"])\n        assert r == [\"步骤1\", \"替代A\", \"替代B\", \"步骤3\", \"步骤4\"], \"  断言失败\"\n        assert r2 == [\"步骤1\", \"步骤2\", \"步骤3\", \"替代X\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: replan_on_failure ═══\")\n    plan = [\"步骤1\", \"步骤2\", \"步骤3\", \"步骤4\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = replan_on_failure(plan, \"步骤2\", [\"替代A\", \"替代B\"])\n        r2 = replan_on_failure(plan, \"步骤4\", [\"替代X\"])\n        assert r == [\"步骤1\", \"替代A\", \"替代B\", \"步骤3\", \"步骤4\"], \"  断言失败\"\n        assert r2 == [\"步骤1\", \"步骤2\", \"步骤3\", \"替代X\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_9(student_func):\n    plan = [\"步骤1\", \"步骤2\", \"步骤3\", \"步骤4\"]\n    r = student_func(plan, \"步骤2\", [\"替代A\", \"替代B\"])\n    assert r == [\"步骤1\", \"替代A\", \"替代B\", \"步骤3\", \"步骤4\"]\n    r2 = student_func(plan, \"步骤4\", [\"替代X\"])\n    assert r2 == [\"步骤1\", \"步骤2\", \"步骤3\", \"替代X\"]\n    return True, \"关卡通过：正确实现动态重规划\""
  },
  {
    "id": "3-10",
    "title": "函数调用 Function Calling 机制",
    "module": "模块三 · 智能体四大核心能力模块 — 行动模块",
    "description": "Function Calling（Tool Use）是智能体行动模块的标准机制：LLM 输出结构化 JSON\n指定要调用的函数名和参数，由宿主程序执行后返回结果。\n\n编写函数 `parse_function_call(llm_response)`：\n- 从 LLM 的 JSON 响应中解析函数调用\n- 模拟执行对应函数并返回结果\n- 支持的函数：get_weather(city), calculate(expr), search(query)",
    "input_output": "输入：llm_response(dict) 如 {\"function\":\"get_weather\",\"params\":{\"city\":\"北京\"}}\n输出：{\"function\": str, \"params\": dict, \"result\": any}",
    "skeleton_code": "# ==========你的代码开始==========\ndef parse_function_call(llm_response):\n    \"\"\"\n    解析 LLM 的函数调用 JSON 并模拟执行。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 根据 func_name 调用对应模拟函数\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parse_function_call ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = parse_function_call({\"function\": \"get_weather\", \"params\": {\"city\": \"北京\"}})\n        r2 = parse_function_call({\"function\": \"calculate\", \"params\": {\"expr\": \"3+5*2\"}})\n        r3 = parse_function_call({\"function\": \"search\", \"params\": {\"query\": \"AI\"}})\n        assert \"北京\" in str(r1[\"result\"]) and \"25\" in str(r1[\"result\"]), \"  断言失败\"\n        assert r2[\"result\"] == 13, \"  断言失败\"\n        assert \"AI\" in str(r3[\"result\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parse_function_call ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = parse_function_call({\"function\": \"get_weather\", \"params\": {\"city\": \"北京\"}})\n        r2 = parse_function_call({\"function\": \"calculate\", \"params\": {\"expr\": \"3+5*2\"}})\n        r3 = parse_function_call({\"function\": \"search\", \"params\": {\"query\": \"AI\"}})\n        assert \"北京\" in str(r1[\"result\"]) and \"25\" in str(r1[\"result\"]), \"  断言失败\"\n        assert r2[\"result\"] == 13, \"  断言失败\"\n        assert \"AI\" in str(r3[\"result\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_10(student_func):\n    r1 = student_func({\"function\": \"get_weather\", \"params\": {\"city\": \"北京\"}})\n    assert \"北京\" in str(r1[\"result\"]) and \"25\" in str(r1[\"result\"])\n    r2 = student_func({\"function\": \"calculate\", \"params\": {\"expr\": \"3+5*2\"}})\n    assert r2[\"result\"] == 13\n    r3 = student_func({\"function\": \"search\", \"params\": {\"query\": \"AI\"}})\n    assert \"AI\" in str(r3[\"result\"])\n    return True, \"关卡通过：正确实现Function Calling\""
  },
  {
    "id": "3-11",
    "title": "外部工具集成",
    "module": "模块三 · 智能体四大核心能力模块 — 行动模块",
    "description": "智能体需要集成多种外部工具：搜索引擎、计算器、数据库查询等。本关实现一个\n工具注册表，智能体能根据任务自动选择并调用对应工具。\n\n编写类 `ToolRegistry`：\n- `register(name, func)`：注册工具\n- `call(name, **kwargs)`：调用已注册的工具\n- `list_tools()`：列出所有可用工具名",
    "input_output": "创建 registry，注册 2-3 个工具，调用并返回结果",
    "skeleton_code": "class ToolRegistry:\n    def __init__(self):\n        self.tools = {}\n\n    # ==========你的代码开始==========\n    def register(self, name, func):\n        \"\"\"注册一个工具\"\"\"\n        # TODO\n        pass\n\n    def call(self, name, **kwargs):\n        \"\"\"调用工具\"\"\"\n        # TODO\n        pass\n\n    def list_tools(self):\n        \"\"\"列出所有工具名\"\"\"\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ToolRegistry ═══\")\n    obj = ToolRegistry()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register(\"add\", lambda a, b: a + b)\n        obj.register(\"greet\", lambda name: f\"Hello, {name}\")\n        assert \"add\" in obj.list_tools(), \"  断言失败\"\n        assert obj.call(\"add\", a=3, b=5) == 8, \"  断言失败\"\n        assert obj.call(\"greet\", name=\"World\") == \"Hello, World\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ToolRegistry ═══\")\n    obj = ToolRegistry()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register(\"add\", lambda a, b: a + b)\n        obj.register(\"greet\", lambda name: f\"Hello, {name}\")\n        assert \"add\" in obj.list_tools(), \"  断言失败\"\n        assert obj.call(\"add\", a=3, b=5) == 8, \"  断言失败\"\n        assert obj.call(\"greet\", name=\"World\") == \"Hello, World\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_11(module_dict):\n    TR = module_dict.get(\"ToolRegistry\")\n    assert TR is not None\n    registry = TR()\n    registry.register(\"add\", lambda a, b: a + b)\n    registry.register(\"greet\", lambda name: f\"Hello, {name}\")\n    assert \"add\" in registry.list_tools()\n    assert registry.call(\"add\", a=3, b=5) == 8\n    assert registry.call(\"greet\", name=\"World\") == \"Hello, World\"\n    return True, \"关卡通过：正确实现工具注册表\""
  },
  {
    "id": "3-12",
    "title": "沙箱安全执行代码",
    "module": "模块三 · 智能体四大核心能力模块 — 行动模块",
    "description": "智能体需要在受限的沙箱环境中安全执行代码，防止恶意代码访问文件系统或网络。\n\n编写函数 `sandbox_execute(code, allowed_builtins=None)`：\n- 仅允许使用白名单中的内置函数（默认：print, len, range, int, float, str, list, dict, abs, min, max, sum）\n- 禁止 __import__、open、eval、exec 等危险函数\n- 用受限的 globals 字典执行代码\n- 返回执行结果字符串（captured stdout）或错误信息",
    "input_output": "输入：code(str), allowed_builtins(set or None)\n输出：{\"success\": bool, \"output\": str}",
    "skeleton_code": "import io\nimport sys\n\n# ==========你的代码开始==========\ndef sandbox_execute(code, allowed_builtins=None):\n    \"\"\"\n    在受限环境中执行 Python 代码。\n    \"\"\"\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: sandbox_execute ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = sandbox_execute(\"print('hello sandbox')\")\n        r2 = sandbox_execute(\"print(len([1,2,3]))\")\n        r3 = sandbox_execute(\"open('/etc/passwd')\")\n        assert r1[\"success\"] and \"hello sandbox\" in r1[\"output\"], \"  断言失败\"\n        assert r2[\"success\"] and \"3\" in r2[\"output\"], \"  断言失败\"\n        assert not r3[\"success\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: sandbox_execute ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = sandbox_execute(\"print('hello sandbox')\")\n        r2 = sandbox_execute(\"print(len([1,2,3]))\")\n        r3 = sandbox_execute(\"open('/etc/passwd')\")\n        assert r1[\"success\"] and \"hello sandbox\" in r1[\"output\"], \"  断言失败\"\n        assert r2[\"success\"] and \"3\" in r2[\"output\"], \"  断言失败\"\n        assert not r3[\"success\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_12(student_func):\n    r1 = student_func(\"print('hello sandbox')\")\n    assert r1[\"success\"] and \"hello sandbox\" in r1[\"output\"]\n    r2 = student_func(\"print(len([1,2,3]))\")\n    assert r2[\"success\"] and \"3\" in r2[\"output\"]\n    r3 = student_func(\"open('/etc/passwd')\")\n    assert not r3[\"success\"], \"open应被禁止\"\n    return True, \"关卡通过：正确实现沙箱执行\""
  },
  {
    "id": "3-13",
    "title": "工具链编排设计",
    "module": "模块三 · 智能体四大核心能力模块 — 行动模块",
    "description": "复杂任务需要多个工具按顺序协作：工具A的输出是工具B的输入，形成工具链。需要\n处理工具间的依赖关系和错误传递。\n\n编写函数 `execute_tool_chain(tool_chain, initial_input)`：\n- tool_chain：[{name, func, error_handling}]\n- 顺序执行每个工具，前一个的输出作为后一个的输入\n- 如果某工具设置了 error_handling=\"skip\"，出错时跳过并保持输入不变\n- 返回最终结果和每步的执行状态",
    "input_output": "输入：tool_chain(list[dict]), initial_input(any)\n输出：{\"final_result\": any, \"steps\": [{\"tool\":..., \"ok\":..., \"output\":...}, ...]}",
    "skeleton_code": "# ==========你的代码开始==========\ndef execute_tool_chain(tool_chain, initial_input):\n    \"\"\"\n    顺序执行工具链，支持错误跳过。\n    \"\"\"\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: execute_tool_chain ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = execute_tool_chain(chain, 5)\n        assert r[\"final_result\"] == \"20\"\n        assert len(r[\"steps\"]) == 3 and all(s[\"ok\"] for s in r[\"steps\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: execute_tool_chain ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = execute_tool_chain(chain, 5)\n        assert r[\"final_result\"] == \"20\"\n        assert len(r[\"steps\"]) == 3 and all(s[\"ok\"] for s in r[\"steps\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_13(student_func):\n    chain = [\n        {\"name\": \"double\", \"func\": lambda x: x * 2, \"error_handling\": \"stop\"},\n        {\"name\": \"add10\", \"func\": lambda x: x + 10, \"error_handling\": \"skip\"},\n        {\"name\": \"to_str\", \"func\": lambda x: str(x), \"error_handling\": \"stop\"},\n    ]\n    r = student_func(chain, 5)\n    assert r[\"final_result\"] == \"20\", f\"5→10→20→'20', 得到{r['final_result']}\"\n    assert len(r[\"steps\"]) == 3 and all(s[\"ok\"] for s in r[\"steps\"])\n    return True, \"关卡通过：正确实现工具链编排\""
  },
  {
    "id": "3-14",
    "title": "人在回路 Human-in-the-Loop",
    "module": "模块三 · 智能体四大核心能力模块 — 行动模块",
    "description": "人在回路（HITL）机制：在智能体执行高风险操作前，暂停并等待人类确认。\n本关模拟 HITL 流程。\n\n编写函数 `hitl_gate(action, risk_level, threshold=7)`：\n- 如果 risk_level >= threshold，返回 {\"approved\": False, \"reason\": \"需人工审批\"}\n- 如果 risk_level < threshold，返回 {\"approved\": True, \"action\": action}\n- risk_level 为 1-10 的整数",
    "input_output": "输入：action(str), risk_level(int), threshold(int)\n输出：{\"approved\": bool, ...}",
    "skeleton_code": "# ==========你的代码开始==========\ndef hitl_gate(action, risk_level, threshold=7):\n    \"\"\"\n    人在回路审批门控。\n    \"\"\"\n    # TODO: 根据风险等级判断是否需要人工审批\n    pass\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: hitl_gate ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = hitl_gate(\"删除临时文件\", 2)\n        r2 = hitl_gate(\"删除生产数据库\", 9)\n        r3 = hitl_gate(\"修改配置\", 7)\n        assert r1[\"approved\"] == True, \"  断言失败\"\n        assert r2[\"approved\"] == False, \"  断言失败\"\n        assert \"审批\" in r2.get(\"reason\", \"\"), \"  断言失败\"\n        assert r3[\"approved\"] == False\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: hitl_gate ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = hitl_gate(\"删除临时文件\", 2)\n        r2 = hitl_gate(\"删除生产数据库\", 9)\n        r3 = hitl_gate(\"修改配置\", 7)\n        assert r1[\"approved\"] == True, \"  断言失败\"\n        assert r2[\"approved\"] == False, \"  断言失败\"\n        assert \"审批\" in r2.get(\"reason\", \"\"), \"  断言失败\"\n        assert r3[\"approved\"] == False\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_3_14(student_func):\n    r1 = student_func(\"删除临时文件\", 2)\n    assert r1[\"approved\"] == True\n    r2 = student_func(\"删除生产数据库\", 9)\n    assert r2[\"approved\"] == False\n    assert \"审批\" in r2.get(\"reason\", \"\")\n    r3 = student_func(\"修改配置\", 7)\n    assert r3[\"approved\"] == False, \"阈值7及以上应拒绝\"\n    return True, \"关卡通过：正确实现HITL门控\""
  },
  {
    "id": "4-1",
    "title": "LangChain 核心组件模拟",
    "module": "模块四 · 开发框架与工程实践",
    "description": "LangChain 的三大核心组件：Chain（链式调用）、Retriever（检索器）、Memory（记忆）。\n本关模拟 Chain 的基本工作原理：将多个处理步骤串联成流水线。\n\n编写类 `SimpleChain`：\n- `add_step(name, func)`：添加一个处理步骤\n- `run(input_data)`：按顺序执行所有步骤，每步的输出作为下一步的输入\n- 返回最终结果和执行日志",
    "input_output": "pip install 无需（纯 Python 模拟）\n输入：创建 chain，添加步骤，run(data)\n输出：{\"result\":..., \"log\": [...]}",
    "skeleton_code": "class SimpleChain:\n    \"\"\"模拟 LangChain 的 Chain 组件\"\"\"\n    def __init__(self):\n        self.steps = []\n\n    # ==========你的代码开始==========\n    def add_step(self, name, func):\n        \"\"\"添加处理步骤\"\"\"\n        # TODO\n        pass\n\n    def run(self, input_data):\n        \"\"\"顺序执行所有步骤\"\"\"\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SimpleChain ═══\")\n    obj = SimpleChain()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_step(\"trim\", lambda s: s.strip())\n        obj.add_step(\"upper\", lambda s: s.upper())\n        obj.add_step(\"suffix\", lambda s: s + \"!\")\n        r = obj.run(\"  hello world  \")\n        assert r[\"result\"] == \"HELLO WORLD!\"\n        assert len(r[\"log\"]) == 3, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SimpleChain ═══\")\n    obj = SimpleChain()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_step(\"trim\", lambda s: s.strip())\n        obj.add_step(\"upper\", lambda s: s.upper())\n        obj.add_step(\"suffix\", lambda s: s + \"!\")\n        r = obj.run(\"  hello world  \")\n        assert r[\"result\"] == \"HELLO WORLD!\"\n        assert len(r[\"log\"]) == 3, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_1(module_dict):\n    SC = module_dict.get(\"SimpleChain\")\n    assert SC is not None\n    chain = SC()\n    chain.add_step(\"trim\", lambda s: s.strip())\n    chain.add_step(\"upper\", lambda s: s.upper())\n    chain.add_step(\"suffix\", lambda s: s + \"!\")\n    r = chain.run(\"  hello world  \")\n    assert r[\"result\"] == \"HELLO WORLD!\"\n    assert len(r[\"log\"]) == 3\n    return True, \"关卡通过：正确模拟LangChain\"",
    "raw_starter_code": "class SimpleChain:\n    \"\"\"模拟 LangChain 的 Chain 组件\"\"\"\n    def __init__(self):\n        self.steps = []\n\n    def add_step(self, name, func):\n        \"\"\"添加处理步骤\"\"\"\n        self.steps.append((name, func))\n\n    def run(self, input_data):\n        \"\"\"顺序执行所有步骤\"\"\"\n        log = []\n        result = input_data\n        for name, func in self.steps:\n            result = func(result)\n            log.append(f\"{name}: OK\")\n        return {\"result\": result, \"log\": log}"
  },
  {
    "id": "4-2",
    "title": "LangGraph 状态机编排",
    "module": "模块四 · 开发框架与工程实践",
    "description": "LangGraph 用有向图定义工作流，支持条件分支和循环。本关模拟简单的状态机\n工作流编排。\n\n编写类 `StateGraph`：\n- `add_node(name, func)`：添加节点，func 接收状态并返回修改后的状态\n- `add_edge(from_node, to_node)`：添加有向边\n- `add_conditional_edge(from_node, condition_func, mapping)`：条件分支\n- `run(start_node, initial_state)`：从起始节点运行，沿边传导状态",
    "input_output": "pip install 无需\n输入：构建图，从 start 运行\n输出：最终状态 dict",
    "skeleton_code": "class StateGraph:\n    \"\"\"模拟 LangGraph 状态机\"\"\"\n    def __init__(self):\n        self.nodes = {}\n        self.edges = {}\n        self.conditional_edges = {}\n\n    # ==========你的代码开始==========\n    def add_node(self, name, func):\n        pass  # TODO: 实现此方法\n\n    def add_edge(self, from_node, to_node):\n        pass  # TODO: 实现此方法\n\n    def add_conditional_edge(self, from_node, condition_func, mapping):\n        pass  # TODO: 实现此方法\n\n    def run(self, start_node, initial_state):\n        # TODO: 从起始节点遍历图，应用每个节点的转换\n        pass  # TODO: 实现此方法\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: StateGraph ═══\")\n    obj = StateGraph()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_node(\"start\", lambda s: {**s, \"value\": s[\"value\"] + 1})\n        obj.add_node(\"double\", lambda s: {**s, \"value\": s[\"value\"] * 2})\n        obj.add_node(\"done\", lambda s: s)\n        obj.add_edge(\"start\", \"double\")\n        obj.add_edge(\"double\", \"done\")\n        r = obj.run(\"start\", {\"value\": 5})\n        assert r[\"value\"] == 12\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: StateGraph ═══\")\n    obj = StateGraph()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_node(\"start\", lambda s: {**s, \"value\": s[\"value\"] + 1})\n        obj.add_node(\"double\", lambda s: {**s, \"value\": s[\"value\"] * 2})\n        obj.add_node(\"done\", lambda s: s)\n        obj.add_edge(\"start\", \"double\")\n        obj.add_edge(\"double\", \"done\")\n        r = obj.run(\"start\", {\"value\": 5})\n        assert r[\"value\"] == 12\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_2(module_dict):\n    SG = module_dict.get(\"StateGraph\")\n    assert SG is not None\n    graph = SG()\n    graph.add_node(\"start\", lambda s: {**s, \"value\": s[\"value\"] + 1})\n    graph.add_node(\"double\", lambda s: {**s, \"value\": s[\"value\"] * 2})\n    graph.add_node(\"done\", lambda s: s)\n    graph.add_edge(\"start\", \"double\")\n    graph.add_edge(\"double\", \"done\")\n    r = graph.run(\"start\", {\"value\": 5})\n    assert r[\"value\"] == 12, f\"5→6→12, 得到{r['value']}\"\n    return True, \"关卡通过：正确模拟LangGraph状态机\""
  },
  {
    "id": "4-3",
    "title": "LlamaIndex RAG 框架模拟",
    "module": "模块四 · 开发框架与工程实践",
    "description": "LlamaIndex 专注于数据索引和检索增强生成（RAG）。核心流程：加载文档→分块\n→向量化→建索引→检索。本关模拟简化的索引-检索流程。\n\n编写类 `SimpleIndex`：\n- `add_documents(docs)`：存储文档列表\n- `build_index()`：构建倒排索引（词→文档ID列表）\n- `search(query, top_k=3)`：基于关键词匹配检索最相关文档",
    "input_output": "输入：创建索引，添加文档，搜索\n输出：相关文档列表",
    "skeleton_code": "class SimpleIndex:\n    \"\"\"模拟 LlamaIndex 文档索引与检索\"\"\"\n    def __init__(self):\n        self.docs = []\n        self.index = {}\n\n    # ==========你的代码开始==========\n    def add_documents(self, docs):\n        \"\"\"docs: list[str]\"\"\"\n        # TODO\n        pass\n\n    def build_index(self):\n        \"\"\"构建倒排索引\"\"\"\n        # TODO: 对每个文档分词（按空白字符split），建立词→文档ID映射\n        pass\n\n    def search(self, query, top_k=3):\n        \"\"\"检索最相关文档\"\"\"\n        # TODO: query分词，统计每个文档的命中次数，返回top_k\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SimpleIndex ═══\")\n    obj = SimpleIndex()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_documents([\"AI智能体是未来趋势\", \"Python是AI的主要编程语言\", \"深度学习需要大量数据\", \"智能体框架如LangChain简化开发\"])\n        obj.build_index()\n        r = obj.search(\"智能体 AI\", top_k=2)\n        assert len(r) == 2, \"  断言失败\"\n        assert \"AI智能体是未来趋势\" in r, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SimpleIndex ═══\")\n    obj = SimpleIndex()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_documents([\"AI智能体是未来趋势\", \"Python是AI的主要编程语言\", \"深度学习需要大量数据\", \"智能体框架如LangChain简化开发\"])\n        obj.build_index()\n        r = obj.search(\"智能体 AI\", top_k=2)\n        assert len(r) == 2, \"  断言失败\"\n        assert \"AI智能体是未来趋势\" in r, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_3(module_dict):\n    SI = module_dict.get(\"SimpleIndex\")\n    assert SI is not None\n    idx = SI()\n    idx.add_documents([\"AI智能体是未来趋势\", \"Python是AI的主要编程语言\", \"深度学习需要大量数据\", \"智能体框架如LangChain简化开发\"])\n    idx.build_index()\n    r = idx.search(\"智能体 AI\", top_k=2)\n    assert len(r) == 2\n    assert \"AI智能体是未来趋势\" in r\n    return True, \"关卡通过：正确模拟索引检索\""
  },
  {
    "id": "4-4",
    "title": "CrewAI 多智能体协作角色定义",
    "module": "模块四 · 开发框架与工程实践",
    "description": "CrewAI 是多智能体协作框架，核心概念：Agent（有角色和目标的智能体）、Task\n（委派给 Agent 的任务）、Crew（协调多 Agent 协作）。\n\n编写类 `CrewAI` 模拟：\n- `create_agent(name, role, goal)`：创建一个 Agent\n- `create_task(description, assigned_agent_name)`：创建任务\n- `execute()`：顺序执行所有任务，返回每个 Agent 的执行结果",
    "input_output": "输入：创建多个 Agent 和 Task\n输出：{\"results\": [{\"agent\":..., \"task\":..., \"output\":...}, ...]}",
    "skeleton_code": "class CrewAI:\n    \"\"\"模拟 CrewAI 多智能体协作\"\"\"\n    def __init__(self):\n        self.agents = {}\n        self.tasks = []\n\n    # ==========你的代码开始==========\n    def create_agent(self, name, role, goal):\n        pass  # TODO: 实现此方法\n\n    def create_task(self, description, assigned_agent_name):\n        pass  # TODO: 实现此方法\n\n    def execute(self):\n        # TODO: 每个Agent执行分配给它的任务\n        pass  # TODO: 实现此方法\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: CrewAI ═══\")\n    obj = CrewAI()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.create_agent(\"研究员\", \"研究\", \"搜索AI资料\")\n        obj.create_agent(\"工程师\", \"开发\", \"编写AI代码\")\n        obj.create_task(\"分析最新AI论文\", \"研究员\")\n        obj.create_task(\"实现原型系统\", \"工程师\")\n        r = obj.execute()\n        assert len(r[\"results\"]) == 2, \"  断言失败\"\n        assert r[\"results\"][0][\"agent\"] == \"研究员\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: CrewAI ═══\")\n    obj = CrewAI()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.create_agent(\"研究员\", \"研究\", \"搜索AI资料\")\n        obj.create_agent(\"工程师\", \"开发\", \"编写AI代码\")\n        obj.create_task(\"分析最新AI论文\", \"研究员\")\n        obj.create_task(\"实现原型系统\", \"工程师\")\n        r = obj.execute()\n        assert len(r[\"results\"]) == 2, \"  断言失败\"\n        assert r[\"results\"][0][\"agent\"] == \"研究员\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_4(module_dict):\n    Crew = module_dict.get(\"CrewAI\")\n    assert Crew is not None\n    crew = Crew()\n    crew.create_agent(\"研究员\", \"研究\", \"搜索AI资料\")\n    crew.create_agent(\"工程师\", \"开发\", \"编写AI代码\")\n    crew.create_task(\"分析最新AI论文\", \"研究员\")\n    crew.create_task(\"实现原型系统\", \"工程师\")\n    r = crew.execute()\n    assert len(r[\"results\"]) == 2\n    assert r[\"results\"][0][\"agent\"] == \"研究员\"\n    return True, \"关卡通过：正确模拟CrewAI协作\""
  },
  {
    "id": "4-5",
    "title": "AutoGen 多智能体对话模拟",
    "module": "模块四 · 开发框架与工程实践",
    "description": "微软的 AutoGen 框架支持多个 AI Agent 通过对话协作完成任务。本关模拟两个\nAgent 之间的对话回合。\n\n编写函数 `autogen_dialogue(agent_a, agent_b, topic, rounds=3)`：\n- 模拟两个 Agent 围绕 topic 进行 rounds 轮对话\n- 每轮两个 Agent 各发言一次\n- 返回完整的对话记录列表",
    "input_output": "输入：agent_a(dict), agent_b(dict), topic(str), rounds(int)\n输出：list[{\"speaker\":..., \"message\":...}]",
    "skeleton_code": "# ==========你的代码开始==========\ndef autogen_dialogue(agent_a, agent_b, topic, rounds=3):\n    \"\"\"\n    模拟 AutoGen 双Agent对话。\n    \"\"\"\n    pass  # TODO: 实现此方法\n        # TODO: 模拟两个Agent交替发言\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: autogen_dialogue ═══\")\n    a = {\"name\": \"Alice\", \"role\": \"分析师\"}\n    b = {\"name\": \"Bob\", \"role\": \"工程师\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = autogen_dialogue(a, b, \"AI安全\", rounds=2)\n        assert len(r) == 4\n        assert r[0][\"speaker\"] == \"Alice\"\n        assert r[1][\"speaker\"] == \"Bob\"\n        assert \"AI安全\" in r[0][\"message\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: autogen_dialogue ═══\")\n    a = {\"name\": \"Alice\", \"role\": \"分析师\"}\n    b = {\"name\": \"Bob\", \"role\": \"工程师\"}\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = autogen_dialogue(a, b, \"AI安全\", rounds=2)\n        assert len(r) == 4\n        assert r[0][\"speaker\"] == \"Alice\"\n        assert r[1][\"speaker\"] == \"Bob\"\n        assert \"AI安全\" in r[0][\"message\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_5(student_func):\n    a = {\"name\": \"Alice\", \"role\": \"分析师\"}\n    b = {\"name\": \"Bob\", \"role\": \"工程师\"}\n    r = student_func(a, b, \"AI安全\", rounds=2)\n    assert len(r) == 4, \"2轮应有4条消息\"\n    assert r[0][\"speaker\"] == \"Alice\"\n    assert r[1][\"speaker\"] == \"Bob\"\n    assert \"AI安全\" in r[0][\"message\"]\n    return True, \"关卡通过：正确模拟AutoGen对话\""
  },
  {
    "id": "4-6",
    "title": "RAG 检索增强生成全流程",
    "module": "模块四 · 开发框架与工程实践",
    "description": "RAG 全流程：用户提问 → 检索相关文档 → 将文档拼入 Prompt → LLM 生成答案。\n本关实现完整的 RAG 模拟流程。\n\n编写函数 `rag_pipeline(query, documents, llm_simulator)`：\n- 用 TF-IDF 简化版检索最相关文档（基于词频）\n- 将检索到的文档拼接为上下文\n- 调用 llm_simulator（一个函数，接收 context+query 返回答案）生成最终回答",
    "input_output": "输入：query(str), documents(list[str]), llm_simulator(callable)\n输出：{\"retrieved\": [...], \"answer\": str}",
    "skeleton_code": "# ==========你的代码开始==========\ndef rag_pipeline(query, documents, llm_simulator):\n    \"\"\"\n    RAG全流程：检索→增强→生成。\n    \"\"\"\n    # 简易TF-IDF检索\n    pass  # TODO: 实现此方法\n\n    # 拼接上下文\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: rag_pipeline ═══\")\n    docs = [\"AI智能体可自主决策\", \"Python是AI开发首选语言\", \"北京的天气今天很好\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = rag_pipeline(\"AI智能体编程语言\", docs, fake_llm)\n        assert len(r[\"retrieved\"]) == 2, \"  断言失败\"\n        assert \"AI智能体可自主决策\" in r[\"retrieved\"], \"  断言失败\"\n        assert \"AI开发\" in r[\"answer\"] or \"智能体\" in r[\"answer\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: rag_pipeline ═══\")\n    docs = [\"AI智能体可自主决策\", \"Python是AI开发首选语言\", \"北京的天气今天很好\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = rag_pipeline(\"AI智能体编程语言\", docs, fake_llm)\n        assert len(r[\"retrieved\"]) == 2, \"  断言失败\"\n        assert \"AI智能体可自主决策\" in r[\"retrieved\"], \"  断言失败\"\n        assert \"AI开发\" in r[\"answer\"] or \"智能体\" in r[\"answer\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_6(student_func):\n    docs = [\"AI智能体可自主决策\", \"Python是AI开发首选语言\", \"北京的天气今天很好\"]\n    def fake_llm(ctx, q):\n        return f\"根据检索结果，关于'{q}'的回答是：{ctx[:50]}...\"\n    r = student_func(\"AI智能体编程语言\", docs, fake_llm)\n    assert len(r[\"retrieved\"]) == 2\n    assert \"AI智能体可自主决策\" in r[\"retrieved\"]\n    assert \"AI开发\" in r[\"answer\"] or \"智能体\" in r[\"answer\"]\n    return True, \"关卡通过：正确实现RAG全流程\""
  },
  {
    "id": "4-7",
    "title": "向量数据库选型与相似度检索",
    "module": "模块四 · 开发框架与工程实践",
    "description": "Chroma/Pinecone/Weaviate 是主流向量数据库。本关模拟向量存储和相似度检索。\n\n编写类 `VectorStore`：\n- `add(id, vector, metadata)`：存储向量\n- `search(query_vector, top_k=3)`：用欧几里得距离找最近邻\n- 返回最近的 top_k 个结果的 id 和 metadata",
    "input_output": "输入：创建 store, 添加向量, 搜索\n输出：[{\"id\":..., \"metadata\":..., \"distance\":...}, ...]",
    "skeleton_code": "import math\n\nclass VectorStore:\n    \"\"\"模拟向量数据库\"\"\"\n    # ==========你的代码开始==========\n    def __init__(self):\n        pass  # TODO: 实现此方法\n\n    def add(self, id, vector, metadata=None):\n        # TODO\n        pass\n\n    def search(self, query_vector, top_k=3):\n        # TODO: 计算欧氏距离，返回最近的top_k\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: VectorStore ═══\")\n    obj = VectorStore()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add(\"v1\", [0.1, 0.9], {\"topic\": \"AI\"})\n        obj.add(\"v2\", [0.8, 0.2], {\"topic\": \"编程\"})\n        obj.add(\"v3\", [0.2, 0.8], {\"topic\": \"AI\"})\n        obj.add(\"v4\", [0.9, 0.1], {\"topic\": \"编程\"})\n        r = obj.search([0.15, 0.85], top_k=2)\n        assert len(r) == 2, \"  断言失败\"\n        assert r[0][\"id\"] == \"v1\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: VectorStore ═══\")\n    obj = VectorStore()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add(\"v1\", [0.1, 0.9], {\"topic\": \"AI\"})\n        obj.add(\"v2\", [0.8, 0.2], {\"topic\": \"编程\"})\n        obj.add(\"v3\", [0.2, 0.8], {\"topic\": \"AI\"})\n        obj.add(\"v4\", [0.9, 0.1], {\"topic\": \"编程\"})\n        r = obj.search([0.15, 0.85], top_k=2)\n        assert len(r) == 2, \"  断言失败\"\n        assert r[0][\"id\"] == \"v1\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_7(module_dict):\n    VS = module_dict.get(\"VectorStore\")\n    assert VS is not None\n    store = VS()\n    store.add(\"v1\", [0.1, 0.9], {\"topic\": \"AI\"})\n    store.add(\"v2\", [0.8, 0.2], {\"topic\": \"编程\"})\n    store.add(\"v3\", [0.2, 0.8], {\"topic\": \"AI\"})\n    store.add(\"v4\", [0.9, 0.1], {\"topic\": \"编程\"})\n    r = store.search([0.15, 0.85], top_k=2)\n    assert len(r) == 2\n    assert r[0][\"id\"] == \"v1\", \"v1最接近查询向量\"\n    return True, \"关卡通过：正确实现向量检索\""
  },
  {
    "id": "4-8",
    "title": "文档切块策略与嵌入向量化",
    "module": "模块四 · 开发框架与工程实践",
    "description": "RAG 工程中，文档切块（Chunking）和嵌入（Embedding）是关键预处理步骤。\n切块策略：固定大小切块 + 重叠区；嵌入：将文本转为数值向量。\n\n编写函数 `chunk_and_embed(text, chunk_size, overlap)`：\n- 将文本按 chunk_size 字符切块，相邻块重叠 overlap 字符\n- 对每块生成简易嵌入向量（基于字符频率的256维向量前8维简化）\n- 返回 [{\"chunk\": str, \"vector\": list[float]}, ...]",
    "input_output": "输入：text(str), chunk_size(int), overlap(int)\n输出：list of {\"chunk\":..., \"vector\":...}",
    "skeleton_code": "# ==========你的代码开始==========\ndef chunk_and_embed(text, chunk_size=128, overlap=20):\n    \"\"\"\n    文档切块 + 简易嵌入向量生成。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 滑动窗口切块\n        # 简易嵌入：32维向量（字符统计归一化）\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: chunk_and_embed ═══\")\n    text = \"A\" * 200 + \"B\" * 200\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = chunk_and_embed(text, chunk_size=150, overlap=20)\n        assert len(r) >= 2\n        assert \"A\" in r[0][\"chunk\"], \"  断言失败\"\n        assert len(r[0][\"vector\"]) == 32, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: chunk_and_embed ═══\")\n    text = \"A\" * 200 + \"B\" * 200\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = chunk_and_embed(text, chunk_size=150, overlap=20)\n        assert len(r) >= 2\n        assert \"A\" in r[0][\"chunk\"], \"  断言失败\"\n        assert len(r[0][\"vector\"]) == 32, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_8(student_func):\n    text = \"A\" * 200 + \"B\" * 200\n    r = student_func(text, chunk_size=150, overlap=20)\n    assert len(r) >= 2, \"至少产生2个chunk\"\n    assert \"A\" in r[0][\"chunk\"]\n    assert len(r[0][\"vector\"]) == 32\n    return True, \"关卡通过：正确实现切块与嵌入\""
  },
  {
    "id": "4-9",
    "title": "提示链 Prompt Chaining 模式",
    "module": "模块四 · 开发框架与工程实践",
    "description": "提示链（Prompt Chaining）将复杂任务拆分为多个步骤，每步的输出作为下一步的\n输入。例如：生成大纲 → 逐节扩展 → 润色。\n\n编写函数 `prompt_chain(task_input, steps)`：\n- steps：[(step_name, transform_func), ...]\n- 顺序执行每个转换函数\n- 返回 {\"intermediate\": [...], \"final\": str}",
    "input_output": "输入：task_input(str), steps(list)\n输出：{\"intermediate\": [...], \"final\": str}",
    "skeleton_code": "# ==========你的代码开始==========\ndef prompt_chain(task_input, steps):\n    \"\"\"\n    串行执行提示链的所有步骤。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 顺序执行每步\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: prompt_chain ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = prompt_chain(\"AI智能体简介\", steps)\n        assert len(r[\"intermediate\"]) == 3, \"  断言失败\"\n        assert \"润色完成\" in r[\"final\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: prompt_chain ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = prompt_chain(\"AI智能体简介\", steps)\n        assert len(r[\"intermediate\"]) == 3, \"  断言失败\"\n        assert \"润色完成\" in r[\"final\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_9(student_func):\n    steps = [\n        (\"outline\", lambda s: f\"大纲: {s}\"),\n        (\"expand\", lambda s: s.replace(\"大纲:\", \"详细:\")),\n        (\"polish\", lambda s: s + \" [润色完成]\"),\n    ]\n    r = student_func(\"AI智能体简介\", steps)\n    assert len(r[\"intermediate\"]) == 3\n    assert \"润色完成\" in r[\"final\"]\n    return True, \"关卡通过：正确实现Prompt Chaining\""
  },
  {
    "id": "4-10",
    "title": "路由 Routing 模式",
    "module": "模块四 · 开发框架与工程实践",
    "description": "路由（Routing）根据用户意图将请求分发到不同的处理器。本关实现意图分类\n路由器。\n\n编写函数 `router(intent, handlers)`：\n- intent：用户意图字符串\n- handlers：{意图关键词: handler_function}\n- 匹配最佳 handler（关键词包含匹配），执行并返回结果\n- 无匹配时返回默认兜底结果",
    "input_output": "输入：intent(str), handlers(dict)\n输出：{\"matched\": str, \"result\": any}",
    "skeleton_code": "# ==========你的代码开始==========\ndef router(intent, handlers):\n    \"\"\"\n    根据意图关键词路由到对应处理器。\n    \"\"\"\n    # TODO: 关键词匹配，选最佳handler\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: router ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = router(\"查询天气\", handlers)\n        r2 = router(\"计算 3+5\", handlers)\n        r3 = router(\"播放音乐\", handlers)\n        assert r1[\"matched\"] == \"天气\"\n        assert r2[\"matched\"] == \"计算\" and \"8\" in str(r2[\"result\"]), \"  断言失败\"\n        assert r3[\"matched\"] == \"default\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: router ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = router(\"查询天气\", handlers)\n        r2 = router(\"计算 3+5\", handlers)\n        r3 = router(\"播放音乐\", handlers)\n        assert r1[\"matched\"] == \"天气\"\n        assert r2[\"matched\"] == \"计算\" and \"8\" in str(r2[\"result\"]), \"  断言失败\"\n        assert r3[\"matched\"] == \"default\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_10(student_func):\n    handlers = {\n        \"天气\": lambda q: \"今天晴25°C\",\n        \"计算\": lambda q: str(eval(q.replace(\"计算\",\"\").strip())),\n        \"搜索\": lambda q: f\"搜索'{q}'的结果\",\n    }\n    r1 = student_func(\"查询天气\", handlers)\n    assert r1[\"matched\"] == \"天气\"\n    r2 = student_func(\"计算 3+5\", handlers)\n    assert r2[\"matched\"] == \"计算\" and \"8\" in str(r2[\"result\"])\n    r3 = student_func(\"播放音乐\", handlers)\n    assert r3[\"matched\"] == \"default\"\n    return True, \"关卡通过：正确实现路由模式\""
  },
  {
    "id": "4-11",
    "title": "并行化 Parallelization 模式",
    "module": "模块四 · 开发框架与工程实践",
    "description": "并行化模式同时执行多个独立子任务，然后合并结果。本关模拟多线程并行处理。\n\n编写函数 `parallel_execute(tasks)`：\n- tasks：[(name, func), ...]\n- 串行模拟并行执行（不引入 threading 库，用 for 循环模拟）\n- 每个 task 执行后记录耗时（用 time.sleep 模拟），收集结果\n- 返回 {\"results\": [{\"task\":..., \"output\":...}, ...], \"total_time\": float}",
    "input_output": "输入：tasks(list[tuple])\n输出：结果汇总字典",
    "skeleton_code": "import time\n\n# ==========你的代码开始==========\ndef parallel_execute(tasks):\n    \"\"\"\n    模拟并行执行多个任务。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # 串行模拟并行（Python不用threading简化）\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parallel_execute ═══\")\n    tasks = [(\"A\", lambda: \"结果A\"), (\"B\", lambda: \"结果B\"), (\"C\", lambda: \"结果C\")]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = parallel_execute(tasks)\n        assert len(r[\"results\"]) == 3, \"  断言失败\"\n        assert r[\"total_time\"] >= 0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: parallel_execute ═══\")\n    tasks = [(\"A\", lambda: \"结果A\"), (\"B\", lambda: \"结果B\"), (\"C\", lambda: \"结果C\")]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = parallel_execute(tasks)\n        assert len(r[\"results\"]) == 3, \"  断言失败\"\n        assert r[\"total_time\"] >= 0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_11(student_func):\n    tasks = [(\"A\", lambda: \"结果A\"), (\"B\", lambda: \"结果B\"), (\"C\", lambda: \"结果C\")]\n    r = student_func(tasks)\n    assert len(r[\"results\"]) == 3\n    assert r[\"total_time\"] >= 0\n    return True, \"关卡通过：正确实现并行化模式\""
  },
  {
    "id": "4-12",
    "title": "自我反思 Reflection 模式",
    "module": "模块四 · 开发框架与工程实践",
    "description": "自我反思（Reflection）模式让智能体评估自己的输出质量，并迭代改进。\n流程：生成初版 → 自我评估 → 找出问题 → 改进 → 再次评估，直到满意。\n\n编写函数 `reflection_loop(initial, evaluator, improver, max_iter=3)`：\n- evaluator：评分函数（返回 0-1 分数和问题描述）\n- improver：改进函数（根据问题描述改进内容）\n- 循环直到分数 >= 0.8 或达到最大迭代次数",
    "input_output": "输入：initial(str), evaluator(callable), improver(callable), max_iter(int)\n输出：{\"final\": str, \"iterations\": int, \"final_score\": float}",
    "skeleton_code": "# ==========你的代码开始==========\ndef reflection_loop(initial, evaluator, improver, max_iter=3):\n    \"\"\"\n    自我反思迭代改进循环。\n    \"\"\"\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reflection_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = reflection_loop(\"短\", evaluator, improver, max_iter=3)\n        assert r[\"iterations\"] <= 3, \"  断言失败\"\n        assert r[\"final_score\"] >= 0.8\n        assert len(r[\"final\"]) > 10, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reflection_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = reflection_loop(\"短\", evaluator, improver, max_iter=3)\n        assert r[\"iterations\"] <= 3, \"  断言失败\"\n        assert r[\"final_score\"] >= 0.8\n        assert len(r[\"final\"]) > 10, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_12(student_func):\n    def evaluator(text):\n        length = len(text)\n        if length < 20:\n            return (0.3, \"太短\")\n        elif length < 40:\n            return (0.6, \"还不够详细\")\n        return (0.9, \"OK\")\n    def improver(text, issues):\n        return text + \" \" + \"补充更多细节以解决: \" + issues\n    r = student_func(\"短\", evaluator, improver, max_iter=3)\n    assert r[\"iterations\"] <= 3\n    assert r[\"final_score\"] >= 0.8, f\"最终分数应≥0.8, 得到{r['final_score']}\"\n    assert len(r[\"final\"]) > 10\n    return True, \"关卡通过：正确实现Reflection模式\""
  },
  {
    "id": "4-13",
    "title": "Agent API 工程化部署",
    "module": "模块四 · 开发框架与工程实践",
    "description": "将 Agent 封装为 REST API 服务是工程化的关键。本关模拟 API 请求处理流程：\n鉴权→参数校验→执行→日志记录→返回结果。\n\n编写函数 `api_gateway(request, agent_func, auth_token)`：\n- 鉴权：检查 auth_token 是否为 \"valid_token_2024\"\n- 参数校验：request 必须包含 \"action\" 键\n- 执行：调用 agent_func(request)\n- 日志：记录请求时间、action、结果\n- 返回 {\"status\": 200/401/400, \"result\":...}",
    "input_output": "输入：request(dict), agent_func(callable), auth_token(str)\n输出：{\"status\": int, \"result\": any, \"log\": str}",
    "skeleton_code": "from datetime import datetime\n\n# ==========你的代码开始==========\ndef api_gateway(request, agent_func, auth_token):\n    \"\"\"\n    API 网关：鉴权→校验→执行→日志。\n    \"\"\"\n    # TODO: 实现完整流程\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: api_gateway ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = api_gateway({\"action\": \"query\"}, lambda r: \"OK\", \"valid_token_2024\")\n        r2 = api_gateway({\"action\": \"query\"}, lambda r: \"OK\", \"bad_token\")\n        r3 = api_gateway({}, lambda r: \"OK\", \"valid_token_2024\")\n        assert r1[\"status\"] == 200 and r1[\"result\"] == \"OK\"\n        assert r2[\"status\"] == 401, \"  断言失败\"\n        assert r3[\"status\"] == 400, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: api_gateway ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = api_gateway({\"action\": \"query\"}, lambda r: \"OK\", \"valid_token_2024\")\n        r2 = api_gateway({\"action\": \"query\"}, lambda r: \"OK\", \"bad_token\")\n        r3 = api_gateway({}, lambda r: \"OK\", \"valid_token_2024\")\n        assert r1[\"status\"] == 200 and r1[\"result\"] == \"OK\"\n        assert r2[\"status\"] == 401, \"  断言失败\"\n        assert r3[\"status\"] == 400, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_4_13(student_func):\n    r1 = student_func({\"action\": \"query\"}, lambda r: \"OK\", \"valid_token_2024\")\n    assert r1[\"status\"] == 200 and r1[\"result\"] == \"OK\"\n    r2 = student_func({\"action\": \"query\"}, lambda r: \"OK\", \"bad_token\")\n    assert r2[\"status\"] == 401\n    r3 = student_func({}, lambda r: \"OK\", \"valid_token_2024\")\n    assert r3[\"status\"] == 400\n    return True, \"关卡通过：正确实现API网关\""
  },
  {
    "id": "5-1",
    "title": "多智能体系统定义与适用场景",
    "module": "模块五 · 多智能体系统",
    "description": "多智能体系统（MAS）由多个自主Agent组成，通过协作完成单个Agent无法完成的\n复杂任务。\"单兵作战 vs 集团军作战\"的关键区别：任务可分解性、通信开销、\n鲁棒性。本关实现多Agent任务分配模拟。\n\n编写函数 `assign_tasks_to_agents(tasks, agents)`：\n- 按负载均衡策略分配：每个Agent轮流领取任务\n- 返回分配结果 dict {agent_name: [task1, task2, ...]}",
    "input_output": "输入：tasks(list[str]), agents(list[str])\n输出：dict[str, list[str]]",
    "skeleton_code": "# ==========你的代码开始==========\ndef assign_tasks_to_agents(tasks, agents):\n    \"\"\"\n    轮询（Round-Robin）策略分配任务给Agent。\n    \"\"\"\n    # TODO: 实现轮询分配\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: assign_tasks_to_agents ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = assign_tasks_to_agents([\"任务1\",\"任务2\",\"任务3\",\"任务4\",\"任务5\"], [\"Alice\",\"Bob\"])\n        r2 = assign_tasks_to_agents([\"单任务\"], [\"Solo\"])\n        assert len(r[\"Alice\"]) == 3 and len(r[\"Bob\"]) == 2, \"  断言失败\"\n        assert \"任务1\" in r[\"Alice\"], \"  断言失败\"\n        assert \"任务2\" in r[\"Bob\"], \"  断言失败\"\n        assert r2 == {\"Solo\": [\"单任务\"]}, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: assign_tasks_to_agents ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = assign_tasks_to_agents([\"任务1\",\"任务2\",\"任务3\",\"任务4\",\"任务5\"], [\"Alice\",\"Bob\"])\n        r2 = assign_tasks_to_agents([\"单任务\"], [\"Solo\"])\n        assert len(r[\"Alice\"]) == 3 and len(r[\"Bob\"]) == 2, \"  断言失败\"\n        assert \"任务1\" in r[\"Alice\"], \"  断言失败\"\n        assert \"任务2\" in r[\"Bob\"], \"  断言失败\"\n        assert r2 == {\"Solo\": [\"单任务\"]}, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_1(student_func):\n    r = student_func([\"任务1\",\"任务2\",\"任务3\",\"任务4\",\"任务5\"], [\"Alice\",\"Bob\"])\n    assert len(r[\"Alice\"]) == 3 and len(r[\"Bob\"]) == 2\n    assert \"任务1\" in r[\"Alice\"]\n    assert \"任务2\" in r[\"Bob\"]\n    r2 = student_func([\"单任务\"], [\"Solo\"])\n    assert r2 == {\"Solo\": [\"单任务\"]}\n    return True, \"关卡通过：正确分配任务\""
  },
  {
    "id": "5-2",
    "title": "管理者-执行者 Manager-Worker 模式",
    "module": "模块五 · 多智能体系统",
    "description": "Manager-Worker 是多智能体系统的经典组织架构。Manager 负责任务分解与分配，\nWorker 负责执行具体任务并汇报结果。适合层级化任务。\n\n编写类 `ManagerWorkerSystem`：\n- Manager 有 `delegate(task)` 方法：将任务拆分为子任务分配给 Worker\n- Worker 有 `execute(subtask)` 方法：执行子任务并返回结果\n- `run(task)` 方法：Manager 分配 → Workers 执行 → 汇总返回",
    "input_output": "输入：创建系统，添加 Worker，run 任务\n输出：{\"assignments\": dict, \"results\": dict, \"summary\": str}",
    "skeleton_code": "class ManagerWorkerSystem:\n    \"\"\"Manager-Worker 多智能体架构\"\"\"\n    def __init__(self):\n        self.manager_name = \"Manager\"\n        self.workers = {}\n\n    # ==========你的代码开始==========\n    def add_worker(self, name, skill):\n        pass  # TODO: 实现此方法\n\n    def run(self, task):\n        # Manager 分解任务\n        pass  # TODO: 实现此方法\n        # 分配并执行\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ManagerWorkerSystem ═══\")\n    obj = ManagerWorkerSystem()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_worker(\"Worker1\", \"前端开发\")\n        obj.add_worker(\"Worker2\", \"后端开发\")\n        r = obj.run(\"开发Web应用\")\n        assert len(r[\"results\"]) == 2, \"  断言失败\"\n        assert \"Manager分配2个子任务\" in r[\"summary\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: ManagerWorkerSystem ═══\")\n    obj = ManagerWorkerSystem()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.add_worker(\"Worker1\", \"前端开发\")\n        obj.add_worker(\"Worker2\", \"后端开发\")\n        r = obj.run(\"开发Web应用\")\n        assert len(r[\"results\"]) == 2, \"  断言失败\"\n        assert \"Manager分配2个子任务\" in r[\"summary\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_2(module_dict):\n    MWS = module_dict.get(\"ManagerWorkerSystem\")\n    assert MWS is not None\n    sys = MWS()\n    sys.add_worker(\"Worker1\", \"前端开发\")\n    sys.add_worker(\"Worker2\", \"后端开发\")\n    r = sys.run(\"开发Web应用\")\n    assert len(r[\"results\"]) == 2\n    assert \"Manager分配2个子任务\" in r[\"summary\"]\n    return True, \"关卡通过：正确实现Manager-Worker模式\""
  },
  {
    "id": "5-3",
    "title": "专家小组 Peer-to-Peer 模式",
    "module": "模块五 · 多智能体系统",
    "description": "对等（P2P）模式中，每个Agent地位平等，通过投票或协商达成共识。适合没有\n明确层级的复杂决策场景。\n\n编写函数 `peer_voting(proposals, votes)`：\n- 每个Agent对每个提案投票（0反对~5强烈支持）\n- 汇总每个提案的总分\n- 返回按得分排序的提案列表（得分相同按字母序）",
    "input_output": "输入：proposals(list[str]), votes(dict[str, dict[str, int]])\n     votes格式: {\"AgentA\": {\"proposal1\": 4, \"proposal2\": 3}, ...}\n输出：list[tuple] 如 [(\"方案A\", 15), (\"方案B\", 10)]",
    "skeleton_code": "# ==========你的代码开始==========\ndef peer_voting(proposals, votes):\n    \"\"\"\n    P2P投票决策：汇总所有Agent的投票，排序返回。\n    \"\"\"\n    # TODO: 计算每个提案的总分，排序\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: peer_voting ═══\")\n    proposals = [\"方案A\", \"方案B\", \"方案C\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = peer_voting(proposals, votes)\n        assert r[0][0] == \"方案A\"\n        assert r[0][1] == 12, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: peer_voting ═══\")\n    proposals = [\"方案A\", \"方案B\", \"方案C\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = peer_voting(proposals, votes)\n        assert r[0][0] == \"方案A\"\n        assert r[0][1] == 12, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_3(student_func):\n    proposals = [\"方案A\", \"方案B\", \"方案C\"]\n    votes = {\n        \"Agent1\": {\"方案A\": 5, \"方案B\": 3, \"方案C\": 1},\n        \"Agent2\": {\"方案A\": 4, \"方案B\": 4, \"方案C\": 2},\n        \"Agent3\": {\"方案A\": 3, \"方案B\": 5, \"方案C\": 4},\n    }\n    r = student_func(proposals, votes)\n    assert r[0][0] == \"方案A\", f\"方案A总分12应排第一, 得到{r[0][0]}\"\n    assert r[0][1] == 12\n    return True, \"关卡通过：正确实现P2P投票\""
  },
  {
    "id": "5-4",
    "title": "智能体间通信协议",
    "module": "模块五 · 多智能体系统",
    "description": "多智能体间需要标准通信协议来交换信息。本关模拟简单的消息传递系统，支持\n点对点和广播两种模式。\n\n编写类 `MessageBus`：\n- `register(agent_name)`：注册Agent\n- `send(sender, receiver, content)`：发送点对点消息\n- `broadcast(sender, content)`：向所有Agent广播（除了sender）\n- `inbox(agent_name)`：返回该Agent的未读消息列表",
    "input_output": "输入：注册Agent，发送消息，查看收件箱\n输出：消息列表",
    "skeleton_code": "class MessageBus:\n    \"\"\"多智能体消息总线\"\"\"\n    def __init__(self):\n        self.agents = set()\n        self.messages = {}\n\n    # ==========你的代码开始==========\n    def register(self, agent_name):\n        pass  # TODO: 实现此方法\n\n    def send(self, sender, receiver, content):\n        \"\"\"点对点发送\"\"\"\n        # TODO\n        pass\n\n    def broadcast(self, sender, content):\n        \"\"\"广播给所有其他Agent\"\"\"\n        # TODO\n        pass\n\n    def inbox(self, agent_name):\n        \"\"\"返回未读消息列表\"\"\"\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: MessageBus ═══\")\n    obj = MessageBus()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register(\"AgentA\")\n        obj.register(\"AgentB\")\n        obj.register(\"AgentC\")\n        obj.send(\"AgentA\", \"AgentB\", \"你好B\")\n        obj.broadcast(\"AgentB\", \"大家好\")\n        assert len(obj.inbox(\"AgentB\")) >= 1, \"  断言失败\"\n        assert len(obj.inbox(\"AgentC\")) == 1\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: MessageBus ═══\")\n    obj = MessageBus()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register(\"AgentA\")\n        obj.register(\"AgentB\")\n        obj.register(\"AgentC\")\n        obj.send(\"AgentA\", \"AgentB\", \"你好B\")\n        obj.broadcast(\"AgentB\", \"大家好\")\n        assert len(obj.inbox(\"AgentB\")) >= 1, \"  断言失败\"\n        assert len(obj.inbox(\"AgentC\")) == 1\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_4(module_dict):\n    MB = module_dict.get(\"MessageBus\")\n    assert MB is not None\n    bus = MB()\n    bus.register(\"AgentA\")\n    bus.register(\"AgentB\")\n    bus.register(\"AgentC\")\n    bus.send(\"AgentA\", \"AgentB\", \"你好B\")\n    bus.broadcast(\"AgentB\", \"大家好\")\n    assert len(bus.inbox(\"AgentB\")) >= 1\n    assert len(bus.inbox(\"AgentC\")) == 1, \"广播应被C收到\"\n    return True, \"关卡通过：正确实现消息总线\""
  },
  {
    "id": "5-5",
    "title": "公告板协调模式",
    "module": "模块五 · 多智能体系统",
    "description": "公告板（Blackboard）模式：所有Agent共享一个公共信息空间，Agent可以写入\n信息（Post）和读取信息（Read），实现松耦合协作。\n\n编写类 `Blackboard`：\n- `post(agent, topic, data)`：发布信息到公告板\n- `read(agent, topic=None)`：读取指定topic的信息（topic=None返回全部）\n- `clear_topic(topic)`：清理某个topic",
    "input_output": "输入：创建公告板，发布/读取信息\n输出：读取到的信息列表",
    "skeleton_code": "class Blackboard:\n    \"\"\"共享公告板\"\"\"\n    def __init__(self):\n        self.board = {}\n\n    # ==========你的代码开始==========\n    def post(self, agent, topic, data):\n        # TODO\n        pass\n\n    def read(self, agent, topic=None):\n        # TODO\n        pass\n\n    def clear_topic(self, topic):\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: Blackboard ═══\")\n    obj = Blackboard()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.post(\"AgentA\", \"vision\", \"检测到障碍物\")\n        obj.post(\"AgentB\", \"vision\", \"确认障碍物\")\n        obj.post(\"AgentC\", \"planning\", \"规划绕行路线\")\n        r = obj.read(\"AgentA\", topic=\"vision\")\n        obj.clear_topic(\"vision\")\n        assert len(r) == 2, \"  断言失败\"\n        assert len(obj.read(\"AgentA\", topic=\"vision\")) == 0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: Blackboard ═══\")\n    obj = Blackboard()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.post(\"AgentA\", \"vision\", \"检测到障碍物\")\n        obj.post(\"AgentB\", \"vision\", \"确认障碍物\")\n        obj.post(\"AgentC\", \"planning\", \"规划绕行路线\")\n        r = obj.read(\"AgentA\", topic=\"vision\")\n        obj.clear_topic(\"vision\")\n        assert len(r) == 2, \"  断言失败\"\n        assert len(obj.read(\"AgentA\", topic=\"vision\")) == 0, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_5(module_dict):\n    BB = module_dict.get(\"Blackboard\")\n    assert BB is not None\n    bb = BB()\n    bb.post(\"AgentA\", \"vision\", \"检测到障碍物\")\n    bb.post(\"AgentB\", \"vision\", \"确认障碍物\")\n    bb.post(\"AgentC\", \"planning\", \"规划绕行路线\")\n    r = bb.read(\"AgentA\", topic=\"vision\")\n    assert len(r) == 2\n    bb.clear_topic(\"vision\")\n    assert len(bb.read(\"AgentA\", topic=\"vision\")) == 0\n    return True, \"关卡通过：正确实现公告板模式\""
  },
  {
    "id": "5-6",
    "title": "订阅/发布事件驱动模式",
    "module": "模块五 · 多智能体系统",
    "description": "发布/订阅（Pub/Sub）模式：Agent 订阅感兴趣的事件类型，当事件发生时自动\n通知所有订阅者。适合事件驱动的协作场景。\n\n编写类 `PubSub`：\n- `subscribe(agent, event_type)`：订阅事件类型\n- `unsubscribe(agent, event_type)`：取消订阅\n- `publish(event_type, data)`：发布事件，通知所有订阅者\n- 返回被通知的 Agent 列表",
    "input_output": "输入：创建PubSub，订阅/发布\n输出：{\"notified\": [\"Agent1\",\"Agent2\"]}",
    "skeleton_code": "class PubSub:\n    \"\"\"发布/订阅事件系统\"\"\"\n    def __init__(self):\n        self.subscriptions = {}\n\n    # ==========你的代码开始==========\n    def subscribe(self, agent, event_type):\n        # TODO\n        pass\n\n    def unsubscribe(self, agent, event_type):\n        # TODO\n        pass\n\n    def publish(self, event_type, data):\n        # TODO: 通知所有订阅者\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: PubSub ═══\")\n    obj = PubSub()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.subscribe(\"AgentA\", \"alarm\")\n        obj.subscribe(\"AgentB\", \"alarm\")\n        obj.subscribe(\"AgentC\", \"info\")\n        r = obj.publish(\"alarm\", \"温度过高\")\n        obj.unsubscribe(\"AgentA\", \"alarm\")\n        r2 = obj.publish(\"alarm\", \"再次告警\")\n        assert \"AgentA\" in r[\"notified\"] and \"AgentB\" in r[\"notified\"], \"  断言失败\"\n        assert \"AgentC\" not in r[\"notified\"], \"  断言失败\"\n        assert \"AgentA\" not in r2[\"notified\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: PubSub ═══\")\n    obj = PubSub()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.subscribe(\"AgentA\", \"alarm\")\n        obj.subscribe(\"AgentB\", \"alarm\")\n        obj.subscribe(\"AgentC\", \"info\")\n        r = obj.publish(\"alarm\", \"温度过高\")\n        obj.unsubscribe(\"AgentA\", \"alarm\")\n        r2 = obj.publish(\"alarm\", \"再次告警\")\n        assert \"AgentA\" in r[\"notified\"] and \"AgentB\" in r[\"notified\"], \"  断言失败\"\n        assert \"AgentC\" not in r[\"notified\"], \"  断言失败\"\n        assert \"AgentA\" not in r2[\"notified\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_6(module_dict):\n    PS = module_dict.get(\"PubSub\")\n    assert PS is not None\n    ps = PS()\n    ps.subscribe(\"AgentA\", \"alarm\")\n    ps.subscribe(\"AgentB\", \"alarm\")\n    ps.subscribe(\"AgentC\", \"info\")\n    r = ps.publish(\"alarm\", \"温度过高\")\n    assert \"AgentA\" in r[\"notified\"] and \"AgentB\" in r[\"notified\"]\n    assert \"AgentC\" not in r[\"notified\"]\n    ps.unsubscribe(\"AgentA\", \"alarm\")\n    r2 = ps.publish(\"alarm\", \"再次告警\")\n    assert \"AgentA\" not in r2[\"notified\"]\n    return True, \"关卡通过：正确实现Pub/Sub模式\""
  },
  {
    "id": "5-7",
    "title": "博弈论纳什均衡入门",
    "module": "模块五 · 多智能体系统",
    "description": "纳什均衡（Nash Equilibrium）是博弈论核心概念：每个参与者在给定他人策略的\n情况下，都选择了自己的最优策略，无人愿意单方面改变策略。\n经典例子：囚徒困境（收益矩阵：双方合作各-1；一方背叛一方合作，背叛者0、\n合作者-10；双方背叛各-5）。\n\n编写函数 `find_nash_equilibrium(payoff_matrix)`：\n- payoff_matrix：2x2 字典 {(A策略,B策略): (A收益,B收益)}\n- 检测纯策略纳什均衡\n- 返回所有纯策略NE的列表",
    "input_output": "输入：payoff_matrix(dict)\n输出：list[tuple] 如 [(\"confess\",\"confess\")]",
    "skeleton_code": "# ==========你的代码开始==========\ndef find_nash_equilibrium(payoff_matrix):\n    \"\"\"\n    找到2x2博弈中的所有纯策略纳什均衡。\n    payoff_matrix: {(\"A1\",\"B1\"): (payoffA, payoffB), ...}\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 对每个策略组合检查是否为纳什均衡\n            # A是否有动机偏离？\n            # B是否有动机偏离？\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: find_nash_equilibrium ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = find_nash_equilibrium(pm)\n        assert len(r) == 1\n        assert r[0] == (\"confess\", \"confess\")\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: find_nash_equilibrium ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = find_nash_equilibrium(pm)\n        assert len(r) == 1\n        assert r[0] == (\"confess\", \"confess\")\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_5_7(student_func):\n    # 囚徒困境\n    pm = {\n        (\"cooperate\", \"cooperate\"): (-1, -1),\n        (\"cooperate\", \"confess\"): (-10, 0),\n        (\"confess\", \"cooperate\"): (0, -10),\n        (\"confess\", \"confess\"): (-5, -5),\n    }\n    r = student_func(pm)\n    assert len(r) == 1, \"囚徒困境只有1个纯策略NE\"\n    assert r[0] == (\"confess\", \"confess\"), \"相互背叛是NE\"\n    return True, \"关卡通过：正确找到纳什均衡\""
  },
  {
    "id": "6-1",
    "title": "AgentBench 综合评估环境",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "AgentBench 是首个系统性评估 LLM 作为 Agent 能力的基准，涵盖 8 大场景：\n操作系统交互、数据库操作、知识图谱推理、网页浏览、编程、游戏、家务模拟、\n工具调用。本关模拟评估流程。\n\n编写函数 `evaluate_agent_on_benchmark(agent_outputs, ground_truth)`：\n- 计算 Pass Rate：agent_output 与 ground_truth 完全匹配的比例\n- 计算 Partial Score：部分匹配得 0.5，完全匹配得 1.0\n- 返回 {\"pass_rate\": float, \"partial_score\": float, \"total\": int, \"passed\": int}",
    "input_output": "输入：agent_outputs(list[str]), ground_truth(list[str])\n输出：评分报告 dict",
    "skeleton_code": "# ==========你的代码开始==========\ndef evaluate_agent_on_benchmark(agent_outputs, ground_truth):\n    \"\"\"\n    评估Agent在基准测试上的表现。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 计算 pass_rate 和 partial_score\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: evaluate_agent_on_benchmark ═══\")\n    outputs = [\"A\", \"B\", \"C\", \"D-partial\"]\n    gt = [\"A\", \"X\", \"C\", \"D\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = evaluate_agent_on_benchmark(outputs, gt)\n        assert r[\"total\"] == 4, \"  断言失败\"\n        assert r[\"passed\"] == 2, \"  断言失败\"\n        assert 0.5 <= r[\"partial_score\"] <= 0.8, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: evaluate_agent_on_benchmark ═══\")\n    outputs = [\"A\", \"B\", \"C\", \"D-partial\"]\n    gt = [\"A\", \"X\", \"C\", \"D\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = evaluate_agent_on_benchmark(outputs, gt)\n        assert r[\"total\"] == 4, \"  断言失败\"\n        assert r[\"passed\"] == 2, \"  断言失败\"\n        assert 0.5 <= r[\"partial_score\"] <= 0.8, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_1(student_func):\n    outputs = [\"A\", \"B\", \"C\", \"D-partial\"]\n    gt = [\"A\", \"X\", \"C\", \"D\"]\n    r = student_func(outputs, gt)\n    assert r[\"total\"] == 4\n    assert r[\"passed\"] == 2\n    assert 0.5 <= r[\"partial_score\"] <= 0.8\n    return True, \"关卡通过：正确评估Agent表现\""
  },
  {
    "id": "6-2",
    "title": "CRAG Benchmark RAG 评估",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "CRAG（Comprehensive RAG Benchmark）专门评估 RAG 系统。核心指标：\n- 检索准确率（Retrieval Accuracy）：检索到的文档中有多少是相关的\n- 生成准确率（Generation Accuracy）：最终答案的正确率\n\n编写函数 `evaluate_rag_system(retrieved_docs, relevant_docs, answers, ground_truth)`：\n- 计算 Recall@k（检索对相关文档的覆盖率）\n- 计算 Answer Accuracy（答案完全匹配率）",
    "input_output": "输入：retrieved_docs(list[list[str]]), relevant_docs(list[set[str]]), answers(list[str]), gt(list[str])\n输出：{\"recall\": float, \"accuracy\": float}",
    "skeleton_code": "# ==========你的代码开始==========\ndef evaluate_rag_system(retrieved_docs, relevant_docs, answers, ground_truth):\n    \"\"\"\n    评估RAG系统的检索和生成质量。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # Recall: 每个查询检索到的相关文档占比\n    # Accuracy: 答案正确率\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: evaluate_rag_system ═══\")\n    ret = [[\"doc1\",\"doc3\"], [\"doc2\"]]\n    rel = [{\"doc1\",\"doc2\",\"doc3\"}, {\"doc2\"}]\n    ans = [\"答案A\", \"答案B\"]\n    gt = [\"答案A\", \"答案C\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = evaluate_rag_system(ret, rel, ans, gt)\n        assert r[\"recall\"] > 0.5, \"  断言失败\"\n        assert r[\"accuracy\"] == 0.5, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: evaluate_rag_system ═══\")\n    ret = [[\"doc1\",\"doc3\"], [\"doc2\"]]\n    rel = [{\"doc1\",\"doc2\",\"doc3\"}, {\"doc2\"}]\n    ans = [\"答案A\", \"答案B\"]\n    gt = [\"答案A\", \"答案C\"]\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = evaluate_rag_system(ret, rel, ans, gt)\n        assert r[\"recall\"] > 0.5, \"  断言失败\"\n        assert r[\"accuracy\"] == 0.5, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_2(student_func):\n    ret = [[\"doc1\",\"doc3\"], [\"doc2\"]]\n    rel = [{\"doc1\",\"doc2\",\"doc3\"}, {\"doc2\"}]\n    ans = [\"答案A\", \"答案B\"]\n    gt = [\"答案A\", \"答案C\"]\n    r = student_func(ret, rel, ans, gt)\n    assert r[\"recall\"] > 0.5\n    assert r[\"accuracy\"] == 0.5\n    return True, \"关卡通过：正确评估RAG系统\""
  },
  {
    "id": "6-3",
    "title": "量化评估推理能力与工具使用",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "评估智能体需要量化多个维度：推理能力（答案推理步骤完整性）、工具使用\n准确率（函数调用正确率）、任务完成率（达标比例）。\n\n编写函数 `multi_dimension_eval(results)`：\n- results：[{reasoning_steps, tool_calls, task_done}]\n- 计算：avg_reasoning_depth（平均推理步数）、tool_accuracy（正确调用比例）、\n  task_completion_rate（完成任务比例）",
    "input_output": "输入：results(list[dict])\n输出：{\"avg_reasoning_depth\": float, \"tool_accuracy\": float, \"completion_rate\": float}",
    "skeleton_code": "# ==========你的代码开始==========\ndef multi_dimension_eval(results):\n    \"\"\"\n    多维度评估智能体能力。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 计算三个维度\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: multi_dimension_eval ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = multi_dimension_eval(results)\n        assert r[\"completion_rate\"] == 2/3, \"  断言失败\"\n        assert r[\"tool_accuracy\"] > 0.5, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: multi_dimension_eval ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = multi_dimension_eval(results)\n        assert r[\"completion_rate\"] == 2/3, \"  断言失败\"\n        assert r[\"tool_accuracy\"] > 0.5, \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_3(student_func):\n    results = [\n        {\"reasoning_steps\": 5, \"tool_calls_correct\": 3, \"tool_calls_total\": 4, \"task_done\": True},\n        {\"reasoning_steps\": 3, \"tool_calls_correct\": 2, \"tool_calls_total\": 2, \"task_done\": True},\n        {\"reasoning_steps\": 2, \"tool_calls_correct\": 0, \"tool_calls_total\": 3, \"task_done\": False},\n    ]\n    r = student_func(results)\n    assert r[\"completion_rate\"] == 2/3\n    assert r[\"tool_accuracy\"] > 0.5\n    return True, \"关卡通过：正确实现多维度评估\""
  },
  {
    "id": "6-4",
    "title": "工具调用权限控制",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "智能体安全治理的核心：对工具调用的权限分级控制。高危操作（删除、修改系统\n配置）需要高级权限，低危操作（读取、查询）普通权限即可。\n\n编写函数 `check_tool_permission(tool_name, user_role, permission_policy)`：\n- permission_policy：{工具名: [允许的角色列表]}\n- 返回 {\"allowed\": True/False, \"reason\": str}",
    "input_output": "输入：tool_name(str), user_role(str), permission_policy(dict)\n输出：{\"allowed\": bool, \"reason\": str}",
    "skeleton_code": "# ==========你的代码开始==========\ndef check_tool_permission(tool_name, user_role, permission_policy):\n    \"\"\"\n    检查用户是否有权限调用指定工具。\n    \"\"\"\n    # TODO: 查找策略，判断角色是否在允许列表中\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: check_tool_permission ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert check_tool_permission(\"read_file\", \"guest\", policy)[\"allowed\"], \"  断言失败\"\n        assert not check_tool_permission(\"delete_system\", \"user\", policy)[\"allowed\"], \"  断言失败\"\n        assert check_tool_permission(\"delete_system\", \"admin\", policy)[\"allowed\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: check_tool_permission ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        assert check_tool_permission(\"read_file\", \"guest\", policy)[\"allowed\"], \"  断言失败\"\n        assert not check_tool_permission(\"delete_system\", \"user\", policy)[\"allowed\"], \"  断言失败\"\n        assert check_tool_permission(\"delete_system\", \"admin\", policy)[\"allowed\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_4(student_func):\n    policy = {\n        \"read_file\": [\"guest\", \"user\", \"admin\"],\n        \"write_file\": [\"user\", \"admin\"],\n        \"delete_system\": [\"admin\"],\n    }\n    assert student_func(\"read_file\", \"guest\", policy)[\"allowed\"]\n    assert not student_func(\"delete_system\", \"user\", policy)[\"allowed\"]\n    assert student_func(\"delete_system\", \"admin\", policy)[\"allowed\"]\n    return True, \"关卡通过：正确实现权限控制\""
  },
  {
    "id": "6-5",
    "title": "幻觉抑制 — 检索增强与约束解码",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "大模型的\"幻觉\"（Hallucination）指生成看似合理但事实错误的内容。抑制方法\n之一：先检索可信知识库，再要求模型严格基于检索结果作答，不编造。\n\n编写函数 `reduce_hallucination(query, knowledge_base, response)`：\n- 检查 response 中是否包含 knowledge_base 之外的新事实声明\n- 将未在知识库中出现的关键实体标记为\"疑似幻觉\"\n- 返回 {\"clean\": bool, \"suspicious\": [str], \"verified_response\": str}",
    "input_output": "输入：query(str), knowledge_base(str), response(str)\n输出：幻觉检测结果",
    "skeleton_code": "# ==========你的代码开始==========\ndef reduce_hallucination(query, knowledge_base, response):\n    \"\"\"\n    检测并标记回答中的疑似幻觉内容。\n    \"\"\"\n    pass  # TODO: 实现此方法\n    # TODO: 找出response中不在knowledge_base中的长词（>3字符）\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reduce_hallucination ═══\")\n    kb = \"AI智能体使用大模型和工具完成自主任务\"\n    resp1 = \"AI智能体使用大模型完成工作\"\n    resp2 = \"AI智能体使用量子计算机完成魔法任务\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = reduce_hallucination(\"什么是AI智能体\", kb, resp1)\n        r2 = reduce_hallucination(\"什么是AI智能体\", kb, resp2)\n        assert r1[\"clean\"]\n        assert not r2[\"clean\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: reduce_hallucination ═══\")\n    kb = \"AI智能体使用大模型和工具完成自主任务\"\n    resp1 = \"AI智能体使用大模型完成工作\"\n    resp2 = \"AI智能体使用量子计算机完成魔法任务\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r1 = reduce_hallucination(\"什么是AI智能体\", kb, resp1)\n        r2 = reduce_hallucination(\"什么是AI智能体\", kb, resp2)\n        assert r1[\"clean\"]\n        assert not r2[\"clean\"]\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_5(student_func):\n    kb = \"AI智能体使用大模型和工具完成自主任务\"\n    resp1 = \"AI智能体使用大模型完成工作\"\n    r1 = student_func(\"什么是AI智能体\", kb, resp1)\n    assert r1[\"clean\"], \"完全基于知识库应无幻觉\"\n    resp2 = \"AI智能体使用量子计算机完成魔法任务\"\n    r2 = student_func(\"什么是AI智能体\", kb, resp2)\n    assert not r2[\"clean\"], \"含编造内容应检测出幻觉\"\n    return True, \"关卡通过：正确实现幻觉检测\""
  },
  {
    "id": "6-6",
    "title": "数据隐私脱敏",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "智能体处理用户数据时必须保护隐私：脱敏（Masking）隐藏敏感信息如身份证号、\n手机号、邮箱地址等。\n\n编写函数 `mask_sensitive_info(text)`：\n- 手机号（11位数字）：138****5678\n- 身份证号（18位）：3****************X\n- 邮箱：u***@domain.com\n- 返回脱敏后的文本",
    "input_output": "输入：text(str)\n输出：脱敏后的 str",
    "skeleton_code": "import re\n\n# ==========你的代码开始==========\ndef mask_sensitive_info(text):\n    \"\"\"\n    对手机号、身份证、邮箱进行脱敏处理。\n    \"\"\"\n    # TODO: 正则替换\n    # 手机号: 1[3-9]\\d{9} → 前3位+****+后4位\n    pass  # TODO: 实现此方法\n    # 身份证: \\d{17}[\\dXx] → 前1位+16星+末位\n    # 邮箱: 用户名部分只保留首字母\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: mask_sensitive_info ═══\")\n    text = \"请联系13812345678或发邮件user@example.com，身份证320102199001011234\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = mask_sensitive_info(text)\n        assert \"13812345678\" not in result\n        assert \"****\" in result, \"  断言失败\"\n        assert \"user@example.com\" not in result\n        assert \"320102199001011234\" not in result\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: mask_sensitive_info ═══\")\n    text = \"请联系13812345678或发邮件user@example.com，身份证320102199001011234\"\n\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        result = mask_sensitive_info(text)\n        assert \"13812345678\" not in result\n        assert \"****\" in result, \"  断言失败\"\n        assert \"user@example.com\" not in result\n        assert \"320102199001011234\" not in result\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_6(student_func):\n    text = \"请联系13812345678或发邮件user@example.com，身份证320102199001011234\"\n    result = student_func(text)\n    assert \"13812345678\" not in result, \"手机号应被脱敏\"\n    assert \"****\" in result\n    assert \"user@example.com\" not in result, \"邮箱应被脱敏\"\n    assert \"320102199001011234\" not in result, \"身份证应被脱敏\"\n    return True, \"关卡通过：正确实现隐私脱敏\""
  },
  {
    "id": "6-7",
    "title": "算法偏见检测与缓解",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "算法偏见（Bias）指模型对特定群体有系统性歧视。简单检测方法：对比不同群体\n的模型输出差异。缓解方法：对输出进行均衡化处理。\n\n编写函数 `detect_bias(predictions, group_labels)`：\n- 计算每个群体的平均得分\n- 如果群体间最大差异 > 阈值（0.15），返回 bias_report\n- 返回 {\"has_bias\": bool, \"group_scores\": dict, \"recommendation\": str}",
    "input_output": "输入：predictions(list[dict 含 score 和 group]), threshold(float)\n输出：偏见检测报告",
    "skeleton_code": "# ==========你的代码开始==========\ndef detect_bias(predictions, threshold=0.15):\n    \"\"\"\n    检测模型输出中的群体偏差。\n    predictions: [{\"score\": 0.8, \"group\": \"A\"}, ...]\n    \"\"\"\n    # TODO: 分组统计平均分，检查组间差异\n    pass  # TODO: 实现此方法\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: detect_bias ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = detect_bias(preds, threshold=0.15)\n        assert r[\"has_bias\"]\n        assert r[\"group_scores\"][\"A\"] > r[\"group_scores\"][\"B\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: detect_bias ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = detect_bias(preds, threshold=0.15)\n        assert r[\"has_bias\"]\n        assert r[\"group_scores\"][\"A\"] > r[\"group_scores\"][\"B\"], \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_7(student_func):\n    preds = [\n        {\"score\": 0.9, \"group\": \"A\"}, {\"score\": 0.88, \"group\": \"A\"},\n        {\"score\": 0.6, \"group\": \"B\"}, {\"score\": 0.62, \"group\": \"B\"},\n    ]\n    r = student_func(preds, threshold=0.15)\n    assert r[\"has_bias\"], \"A组≈0.89, B组≈0.61, 差异>0.15应检测到偏见\"\n    assert r[\"group_scores\"][\"A\"] > r[\"group_scores\"][\"B\"]\n    return True, \"关卡通过：正确实现偏见检测\""
  },
  {
    "id": "6-8",
    "title": "具身智能（Embodied AI）感知-操作循环",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "具身智能体（Embodied Agent）在物理环境中感知和操作。核心循环：感知环境\n→ 解析状态 → 规划动作 → 执行动作 → 观察效果。\n\n编写函数 `embodied_agent_loop(environment, policy, max_steps=5)`：\n- environment：dict，包含 state 和 actions 映射\n- policy：函数，根据 state 返回 action_name\n- 模拟循环直到 state 变为 \"goal\" 或超步数\n- 返回执行轨迹列表",
    "input_output": "输入：environment(dict), policy(callable), max_steps(int)\n输出：[{\"step\": int, \"state\":str, \"action\":str}, ...]",
    "skeleton_code": "# ==========你的代码开始==========\ndef embodied_agent_loop(environment, policy, max_steps=5):\n    \"\"\"\n    具身智能体感知-规划-行动循环。\n    \"\"\"\n    pass  # TODO: 实现此方法\n        # 执行动作，更新状态\n# ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: embodied_agent_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = embodied_agent_loop(env, policy, max_steps=5)\n        assert len(r) >= 2, \"  断言失败\"\n        assert r[-1][\"state\"] == \"goal\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: embodied_agent_loop ═══\")\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        r = embodied_agent_loop(env, policy, max_steps=5)\n        assert len(r) >= 2, \"  断言失败\"\n        assert r[-1][\"state\"] == \"goal\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_8(student_func):\n    env = {\n        \"initial_state\": \"start\",\n        \"transitions\": {(\"start\",\"forward\"): \"mid\", (\"mid\",\"forward\"): \"goal\"}\n    }\n    def policy(state):\n        return \"forward\"\n    r = student_func(env, policy, max_steps=5)\n    assert len(r) >= 2\n    assert r[-1][\"state\"] == \"goal\"\n    return True, \"关卡通过：正确实现具身智能循环\""
  },
  {
    "id": "6-9",
    "title": "MCP 模型上下文协议模拟",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "MCP（Model Context Protocol）是 Anthropic 提出的智能体与外部工具的标准\n连接协议。它定义了智能体如何发现工具、请求工具、接收结果的统一接口。\n\n编写类 `MCPServer` 模拟：\n- `register_tool(name, description, input_schema, handler)`：注册工具\n- `list_tools()`：返回所有已注册工具的声明（name+description+schema）\n- `call_tool(name, arguments)`：调用工具并返回 {\"content\": [...]}",
    "input_output": "pip install 无需\n输入：创建Server，注册工具，调用\n输出：MCP标准响应格式",
    "skeleton_code": "class MCPServer:\n    \"\"\"MCP 协议服务端模拟\"\"\"\n    def __init__(self):\n        self.tools = {}\n\n    # ==========你的代码开始==========\n    def register_tool(self, name, description, input_schema, handler):\n        \"\"\"注册工具: handler接收arguments字典返回结果\"\"\"\n        # TODO\n        pass\n\n    def list_tools(self):\n        \"\"\"返回工具列表\"\"\"\n        # TODO\n        pass\n\n    def call_tool(self, name, arguments):\n        \"\"\"调用指定工具\"\"\"\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: MCPServer ═══\")\n    obj = MCPServer()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register_tool(\"calculator\", \"执行数学运算\", {\"expr\": \"string\"}, lambda args: str(eval(args[\"expr\"])))\n        obj.register_tool(\"weather\", \"查询天气\", {\"city\": \"string\"}, lambda args: f\"{args['city']}: 晴\")\n        tools = obj.list_tools()\n        r = obj.call_tool(\"calculator\", {\"expr\": \"3+5*2\"})\n        assert len(tools) == 2, \"  断言失败\"\n        assert tools[0][\"name\"] in [\"calculator\", \"weather\"], \"  断言失败\"\n        assert \"13\" in str(r[\"content\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: MCPServer ═══\")\n    obj = MCPServer()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register_tool(\"calculator\", \"执行数学运算\", {\"expr\": \"string\"}, lambda args: str(eval(args[\"expr\"])))\n        obj.register_tool(\"weather\", \"查询天气\", {\"city\": \"string\"}, lambda args: f\"{args['city']}: 晴\")\n        tools = obj.list_tools()\n        r = obj.call_tool(\"calculator\", {\"expr\": \"3+5*2\"})\n        assert len(tools) == 2, \"  断言失败\"\n        assert tools[0][\"name\"] in [\"calculator\", \"weather\"], \"  断言失败\"\n        assert \"13\" in str(r[\"content\"]), \"  断言失败\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_9(module_dict):\n    MCP = module_dict.get(\"MCPServer\")\n    assert MCP is not None\n    server = MCP()\n    server.register_tool(\"calculator\", \"执行数学运算\", {\"expr\": \"string\"}, lambda args: str(eval(args[\"expr\"])))\n    server.register_tool(\"weather\", \"查询天气\", {\"city\": \"string\"}, lambda args: f\"{args['city']}: 晴\")\n    tools = server.list_tools()\n    assert len(tools) == 2\n    assert tools[0][\"name\"] in [\"calculator\", \"weather\"]\n    r = server.call_tool(\"calculator\", {\"expr\": \"3+5*2\"})\n    assert \"13\" in str(r[\"content\"])\n    return True, \"关卡通过：正确实现MCP协议\""
  },
  {
    "id": "6-10",
    "title": "Skills 技能模块封装",
    "module": "模块六 · 评估、安全与前沿拓展",
    "description": "Skills 机制将常用能力（如代码执行、文件解析、搜索）封装为可复用的技能模块，\n方便在不同 Agent 间共享。每个 Skill 有 name、description、input_schema 和\nexecute 方法。\n\n编写类 `SkillRegistry`：\n- `register_skill(skill_dict)`：注册技能\n- `find_skill(task_description)`：根据任务描述关键词匹配最佳技能\n- `execute_skill(name, **kwargs)`：执行指定技能",
    "input_output": "输入：创建registry，注册技能，匹配执行\n输出：执行结果",
    "skeleton_code": "class SkillRegistry:\n    \"\"\"技能注册中心\"\"\"\n    def __init__(self):\n        self.skills = {}\n\n    # ==========你的代码开始==========\n    def register_skill(self, name, description, keywords, handler):\n        \"\"\"注册技能及其关键词\"\"\"\n        # TODO\n        pass\n\n    def find_skill(self, task_description):\n        \"\"\"关键词匹配最佳技能，返回skill_name或None\"\"\"\n        # TODO\n        pass\n\n    def execute_skill(self, name, **kwargs):\n        \"\"\"执行已注册的技能\"\"\"\n        # TODO\n        pass\n    # ==========你的代码结束==========\n\n# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SkillRegistry ═══\")\n    obj = SkillRegistry()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register_skill(\"code_exec\", \"安全执行Python代码\", [\"代码\",\"运行\",\"执行\"], lambda code: f\"执行结果: {eval(code)}\")\n        obj.register_skill(\"web_search\", \"搜索网络信息\", [\"搜索\",\"查找\",\"查询\"], lambda query: f\"搜索结果: {query}\")\n        match = obj.find_skill(\"帮我运行一段Python代码\")\n        result = obj.execute_skill(\"code_exec\", code=\"1+1\")\n        match2 = obj.find_skill(\"搜索AI最新论文\")\n        assert match == \"code_exec\"\n        assert \"2\" in str(result), \"  断言失败\"\n        assert match2 == \"web_search\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "test_code": "# ===== 评测代码 — 请实现标记区域内代码后运行 =====\n# 所有测试用例通过才算完成本题\nif __name__ == \"__main__\":\n    passed = 0\n    total = 0\n    errors = []\n\n    print(\"═══ 评测: SkillRegistry ═══\")\n    obj = SkillRegistry()\n    # ── 用例1: 测试1 ──\n    total += 1\n    try:\n        obj.register_skill(\"code_exec\", \"安全执行Python代码\", [\"代码\",\"运行\",\"执行\"], lambda code: f\"执行结果: {eval(code)}\")\n        obj.register_skill(\"web_search\", \"搜索网络信息\", [\"搜索\",\"查找\",\"查询\"], lambda query: f\"搜索结果: {query}\")\n        match = obj.find_skill(\"帮我运行一段Python代码\")\n        result = obj.execute_skill(\"code_exec\", code=\"1+1\")\n        match2 = obj.find_skill(\"搜索AI最新论文\")\n        assert match == \"code_exec\"\n        assert \"2\" in str(result), \"  断言失败\"\n        assert match2 == \"web_search\"\n        print(\"  [PASS]用例1 通过\")\n        passed += 1\n    except Exception as e:\n        print(f\"  [FAIL] 用例1 失败: {e}\")\n        errors.append(f\"用例1: {e}\")\n\n    # ── 评测报告 ──\n    sep = \"=\" * 40\n    print(f\"\\n{sep}\")\n    print(f\"  评测结果: {passed}/{total} 通过\")\n    if errors:\n        print(f\"  失败详情:\")\n        for e in errors:\n            print(f\"    {e}\")\n    if passed == total and total > 0:\n        print(\"  *** 全部通过！恭喜完成本题！ ***\")\n    else:\n        print(\"  [TIP] 请根据失败信息修改代码后重新运行\")",
    "eval_code": "def evaluate_6_10(module_dict):\n    SR = module_dict.get(\"SkillRegistry\")\n    assert SR is not None\n    registry = SR()\n    registry.register_skill(\"code_exec\", \"安全执行Python代码\", [\"代码\",\"运行\",\"执行\"], lambda code: f\"执行结果: {eval(code)}\")\n    registry.register_skill(\"web_search\", \"搜索网络信息\", [\"搜索\",\"查找\",\"查询\"], lambda query: f\"搜索结果: {query}\")\n    match = registry.find_skill(\"帮我运行一段Python代码\")\n    assert match == \"code_exec\", f\"应匹配code_exec, 得到{match}\"\n    result = registry.execute_skill(\"code_exec\", code=\"1+1\")\n    assert \"2\" in str(result)\n    match2 = registry.find_skill(\"搜索AI最新论文\")\n    assert match2 == \"web_search\"\n    return True, \"关卡通过：正确实现技能注册中心\""
  }
]