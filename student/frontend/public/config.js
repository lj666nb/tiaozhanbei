/**
 * 运行时配置 — 部署后可直接修改此文件，无需重新构建前端
 *
 * 使用方式：
 *   1. 如果前后端在同一域名（Docker部署），保持默认值 /api 即可
 *   2. 如果后端在其他地址，修改为完整 URL：
 *      window.__API_BASE_URL__ = 'http://192.168.1.100:8000/api';
 *
 * 注意：修改后刷新页面即可生效
 */
window.__API_BASE_URL__ = '/api';
